---
phase: WLD-01-safety-foundation
plan: 32
type: execute
wave: 4
depends_on: ['01-02']
files_modified:
  - src/process/bridge/projectBridge.ts
  - src/process/task/ConversationTurnCompletionService.ts
  - src/process/task/WCoreManager.ts
  - src/process/task/AcpAgentManager.ts
  - src/process/services/WorkspaceSnapshotService.ts
  - src/process/services/cohort/instrumentation/CohortWorkJourneyAuthority.ts
  - tests/integration/cohort/conversationProjectBindings.test.ts
autonomous: true
requirements: [SAF-02]
must_haves:
  truths:
    [
      'Conversation, Project resume, Cowork artifact, and developer verification terminals originate at process-owned domain authorities.',
    ]
  artifacts:
    - path: src/process/services/cohort/instrumentation/CohortWorkJourneyAuthority.ts
      provides: process-owned correlation and terminal authority for the four work journeys
    - path: tests/integration/cohort/conversationProjectBindings.test.ts
      provides: real-domain binding proof
  key_links:
    - from: src/process/task/ConversationTurnCompletionService.ts
      to: src/process/services/cohort/instrumentation/CohortJourneyObserver.ts
      via: process-owned completion notification
---

<objective>
Bind the observer to the actual conversation-completion, Project-open, durable-workspace, and backend tool-result authorities.

Terminal claim: the first four user-work journeys emit exactly one start/terminal pair from real process-owned lifecycle seams, never from renderer inference.
</objective>
<tasks>
<task type="auto">
<name>Task 1: Wire conversation, Project, artifact, and developer terminals</name>
<files>src/process/bridge/projectBridge.ts, src/process/task/ConversationTurnCompletionService.ts, src/process/task/WCoreManager.ts, src/process/task/AcpAgentManager.ts, src/process/services/WorkspaceSnapshotService.ts, src/process/services/cohort/instrumentation/CohortWorkJourneyAuthority.ts, tests/integration/cohort/conversationProjectBindings.test.ts</files>
<action>Create a process-owned `CohortWorkJourneyAuthority` and bind it only at real production seams: `chat.first-response` after `ConversationTurnCompletionService` loads the persisted conversation and accepts an actual completed turn; `project.resume` inside `project.get` after the process returns the requested persisted Project and its conversation set; `cowork.create-artifact` only after `WorkspaceSnapshotService.compare` observes a durable created/modified file under the correlated workspace; and `developer.modify-and-verify` only after the same correlated change is followed by a successful verification tool result observed by WCoreManager or AcpAgentManager. A renderer invoke, document conversion, ProjectService CRUD call, model prose, tool start, failed command, or uncorrelated file change is never terminal authority. Reject duplicate, missing-start, cross-session, cross-workspace, stale, and out-of-order evidence. Own no locale/generated files.</action>
<verify>GSD_RUNTIME=codex bunx vitest run tests/integration/cohort/conversationProjectBindings.test.ts && bun run typecheck</verify>
<done>All four bindings emit closed observer events from authoritative seams with hostile near-miss coverage.</done>
</task>
</tasks>
<verification>Run `bun run lint -- src/process/bridge/projectBridge.ts src/process/task/ConversationTurnCompletionService.ts src/process/task/WCoreManager.ts src/process/task/AcpAgentManager.ts src/process/services/WorkspaceSnapshotService.ts src/process/services/cohort/instrumentation/CohortWorkJourneyAuthority.ts tests/integration/cohort/conversationProjectBindings.test.ts`, focused integration, typecheck, and exact full `bun run test` before summary or commit.</verification>
<success_criteria>Four primary journeys are process-bound without claiming full instrumentation acceptance.</success_criteria>
<output>Create `01-32-SUMMARY.md` only after the full gate.</output>
