# Intel notarization incident diagnostic

Contract: isolate v0.12.18 Intel invalid-main-signature boundary, starting at 2c4ef68850d4c294b90b06394342fb67ea0402c7. Record post-sign/post-staple executable identity outside the app; compare staged and read-only mounted DMG executable SHA256 and deep/strict signatures before submission. Preserve DMG and diagnostic/build logs separately. No signing flag/order changes, release/tag changes, normal release artifact admission, or other platform rebuilds. One Intel producer via branch-only wrapper after root review; local commit only until then.

Acceptance/checks: focused regression proves equal bytes pass, changed embedded bytes or bad signature fail, cleanup occurs, opt-out does nothing, and hooks run before submission; JavaScript syntax and workflow structure checks. Normal reusable source/capability/signing gates remain. Maximum two verification rounds. Stop after reviewable local commit; native diagnostic result remains pending.

State: implementation prepared; round 1 accepted locally. Six Node regression tests pass (`node --test tests/intel-notary-diagnostic.test.cjs`); JS syntax and git whitespace checks pass; workflow YAML/Intel-only/default-off/no-normal-upload assertions pass. Repository oxlint reports zero warnings/errors (one non-ignored file; scripts are excluded by existing config). An initial ESLint probe was inapplicable: the project uses oxlint. Native Intel diagnostic outcome remains pending. No remote dispatch/push.

Next authorized action: root reviews this local candidate, then explicitly authorizes pushing diagnose/intel-notary-018; the push triggers only the diagnostic wrapper for this branch. Capture producer run/commit identity and inspect the one Intel job. Do not rerun unchanged rejected bytes. Read the post-sign, post-staple and DMG JSON plus full build log from the uniquely named diagnostic artifact; failed DMG is preserved there, never as a normal release artifact. The wrapper performs no release publication. Existing platform artifacts remain untouched.

Wrapper preconditions confirmed: only the parent build-and-release workflow owns automatic retry and actions:write; its dev/tag triggers do not include this branch. The diagnostic directly calls the reusable workflow, whose permissions are actions:read/contents:read. Builder afterSign context.outDir and buildResult.outDir both resolve the configured project/out; the build writer RUNNER_TEMP/build.log matches the upload runner.temp/build.log. Changed-file prek round 1 passed all applicable checks except Oxfmt, which normalized two new diagnostic files. Round 2 confirms those formatting corrections and six focused regressions; native diagnostic remains pending root push review.
