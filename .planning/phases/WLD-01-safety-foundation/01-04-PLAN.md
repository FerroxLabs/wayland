---
phase: WLD-01-safety-foundation
plan: 04
type: execute
wave: 9
depends_on: ["01-03", "01-38"]
files_modified: []
autonomous: false
requirements: [SAF-02, SAF-05]
must_haves:
  truths:
    - "Fourteen real calendar days elapsed without simulation or backfill."
    - "All four cohorts and five journeys have complete denominators."
    - "Any zero-tolerance event or insufficient sample keeps completion red."
  artifacts:
    - path: external-evidence/M0B-OBSERVATION-COMPLETE.json
      provides: authenticated elapsed-observation receipt
  key_links:
    - from: external-evidence/M0B-OBSERVATION-COMPLETE.json
      to: external-evidence/M0B-DAY0.json
      via: immutable window and candidate lineage
---

<objective>
Close the elapsed observation window and prove dataset completeness without yet signing M0B acceptance.

Terminal claim: the exact Day-0 window elapsed for 14 calendar days and produced complete, privacy-bounded denominators with no hidden stop condition.
</objective>

<execution_context>
D-05 and D-06 forbid clock simulation, backfill, and combining observation completion with acceptance signing.
</execution_context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify elapsed time and immutable lineage</name>
  <action>After the receipt end timestamp, authenticate Day-0 lineage, exact build/usability-protocol-digest/cohort identities, and event-chain integrity. Reject changed protocol, changed classification, gaps, duplicate conflicts, pre-start records, post-window records, and any completion attempted before the real end time.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0B-DAY0`; require exact `M0B-DAY0` in green `accepted_targets` before inspecting elapsed lineage. This task does not yet claim observation completion.</verify>
  <done>Elapsed-time and immutable-lineage checks pass against the Day-0 candidate.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Seal completion statistics</name>
  <action>Compute aggregate-only sample counts and denominators for every cohort, journey, session terminal, crash, support, accessibility, and zero-tolerance category. Seal `M0B-OBSERVATION-COMPLETE.json`. Missing minimums or any stop event must produce a failing receipt rather than an acceptance receipt.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0B-OBSERVATION`; require green `prerequisites` for `M0B-DAY0` and green `accepted_targets` for exact `M0B-OBSERVATION-COMPLETE`. Before 01-38, remain BLOCKED.</verify>
  <done>A completion receipt exists only when real elapsed evidence and all completeness gates pass; it does not claim M0B acceptance.</done>
</task>
</tasks>

<threat_model>
STRIDE: signed lineage prevents spoofing/tampering/repudiation; aggregate schema prevents information disclosure; bounded processing handles denial safely; early completion, policy swaps, and backfill cannot elevate evidence state.
</threat_model>

<verification>
Verify externally against the exact Day-0 receipt and UTC wall clock. No rerun-until-green or data deletion.
</verification>

<success_criteria>
Authenticated completion evidence proves the real window and all denominators without minting M0B acceptance.
</success_criteria>

<output>
Create `01-04-SUMMARY.md` with receipt identity or exact blocking category.
</output>
