---
phase: WLD-01-safety-foundation
plan: 10
type: execute
wave: 3
depends_on: ['01-06']
files_modified:
  - contracts/flux-routing-evidence/v1
  - src/common/routingEvidence/v1.ts
  - src/process/flux/FluxRoutingEvidenceAdapter.ts
  - src/process/bridge/index.ts
  - src/process/utils/initBridgeStandalone.ts
  - tests/unit/process/flux/FluxRoutingEvidenceAdapter.test.ts
  - tests/integration/flux/routingEvidenceAbsence.test.ts
autonomous: true
requirements: [FLUX-01, SAF-05]
must_haves:
  truths:
    - 'The merged Flux producer corpus is byte-exact and fully correlated.'
    - 'Fixture route, attempt, retry, fallback, terminal, cost, and degraded evidence replay correctly without implying live delivery.'
    - 'Absent trusted live delivery makes dependent claims physically absent.'
  artifacts:
    - path: contracts/flux-routing-evidence/v1/manifest.json
      provides: immutable producer contract identity
    - path: src/process/flux/FluxRoutingEvidenceAdapter.ts
      provides: corpus replay adapter and capability absence gate
  key_links:
    - from: contracts/flux-routing-evidence/v1
      to: src/process/flux/FluxRoutingEvidenceAdapter.ts
      via: schema/digest verification and production reducer replay
---

<objective>
Seal local Flux corpus construction and the no-live-delivery physical-absence boundary.

Terminal claim: the pinned 12-fixture corpus replays with full correlation, while no trusted delivery means no route/cost/reconciliation claim can appear.
</objective>

<execution_context>
D-09 and D-12 apply. Producer commit `6e7fa427ee6c062b531e67c0e7e1cedfd7aff208` is corpus authority; it does not itself prove live delivery.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Pin and replay the producer corpus</name>
  <files>contracts/flux-routing-evidence/v1, src/process/flux/FluxRoutingEvidenceAdapter.ts, tests/unit/process/flux/FluxRoutingEvidenceAdapter.test.ts</files>
  <action>Own the whole `contracts/flux-routing-evidence/v1` directory and its exact inventory: `manifest.json`, `schema.json`, and the 12 named fixtures (`auto_routing`, `capability_available`, `capability_no_flux`, `circuit_open_degraded`, `disconnected`, `explicit_native`, `fallback`, `invalid_credential`, `retry`, `terminal_failure`, `unpriced_cost`, `user_override`). Recompute all manifest/schema/fixture digests, reject missing/extra files, and replay through `src/common/routingEvidence/v1.ts` and the production adapter. Enforce request/attempt/retry/fallback/response correlation, explicit-native precedence, degraded `no_flux`, terminal behavior, observed usage/cost authority, and unpriced-cost handling. Reject malformed, unknown-critical, duplicate-conflict, gap, out-of-order, and post-terminal evidence.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/process/flux/FluxRoutingEvidenceAdapter.test.ts</verify>
  <done>All producer fixtures and hostile variants replay deterministically with no candidate-minted charge.</done>
</task>
<task type="auto">
  <name>Task 2: Prove trusted live delivery is physically absent</name>
  <files>src/process/bridge/index.ts, src/process/utils/initBridgeStandalone.ts, tests/integration/flux/routingEvidenceAbsence.test.ts</files>
  <action>Keep both production composition roots initialized in explicit `no_flux` state and add a source-grounded integration guard proving that no Desktop, Web/Cloud, ACP, WCore, cost-ledger, or Mission Control callsite supplies a live capability response or producer stream to `FluxRoutingEvidenceAdapter.replay`. Prove the current application cannot expose route explanations, attempt history, Flux cost reconciliation, or debit authority from adapter presence, model claims, catalog estimates, or existing `engine|computed|unknown` cost sources. Any later transport must be introduced as a separately planned contract with correlation, persistence, presentation, and hostile proof; this plan must not invent it.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/integration/flux/routingEvidenceAbsence.test.ts && bun run typecheck</verify>
  <done>Current composition is demonstrably no-live/no-claim; source presence cannot masquerade as trusted Flux delivery.</done>
</task>
</tasks>

<threat_model>
STRIDE: producer/session identity resists spoofing; digest/correlation checks detect tampering; attempts are attributable; evidence omits private account data; partial streams fail safely; model or Desktop estimates cannot elevate accounting authority.
</threat_model>

<verification>
Run `bun run lint -- src/common/routingEvidence/v1.ts src/process/flux/FluxRoutingEvidenceAdapter.ts src/process/bridge/index.ts src/process/utils/initBridgeStandalone.ts tests/unit/process/flux/FluxRoutingEvidenceAdapter.test.ts tests/integration/flux/routingEvidenceAbsence.test.ts`, focused adapter/absence suites, typecheck, and exact full `bun run test` before summary or commit. Plan 01-28 owns external producer/corpus acceptance; plan 01-17 owns the mutually exclusive live-M1F versus physical-absence result.
</verification>

<success_criteria>
FLUX-01 corpus behavior is locally proven while live route/cost claims remain unavailable until separate trusted delivery exists; plan 01-28 owns producer/corpus acceptance.
</success_criteria>

<output>
Create `01-10-SUMMARY.md` only after the full local gate, separating corpus PASS, production-delivery status, and M1F/absence non-claims.
</output>
