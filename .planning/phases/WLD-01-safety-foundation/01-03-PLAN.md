---
phase: WLD-01-safety-foundation
plan: 03
type: execute
wave: 8
depends_on: ["01-23", "01-25", "01-26", "01-38"]
files_modified: []
autonomous: false
requirements: [SAF-02, SAF-05]
must_haves:
  truths:
    - "Observation begins only after accepted cohort and instrumentation receipts."
    - "Sean separately authorizes the real observation."
    - "Day 0 records exact usability-protocol digest, build, cohort, clock, denominator, and decision-owner identities."
  artifacts:
    - path: external-evidence/M0B-DAY0.json
      provides: authenticated observation-start receipt
  key_links:
    - from: external-evidence/M0B-DAY0.json
      to: external accepted-packet registry
      via: exact CAS tuple and authorized signer
---

<objective>
Seal Day 0 and begin the real four-cohort observation only under explicit authorization.

Terminal claim: an authenticated start receipt proves the exact accepted implementation and policy began a real, non-backfilled 14-calendar-day window.
</objective>

<execution_context>
D-02, D-06, D-12, and D-13 are hard gates. Repository-local prose or test clocks cannot start the observation.
</execution_context>

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Authorize observation start</name>
  <action>Present the accepted 01-01 and 01-02 external receipts, exact candidate commit/tree, immutable 01-23 usability-protocol digest, cohort-classification schema, privacy statement, scripts, segments, rubric, thresholds, denominators, sample/soak minimums, Classic comparison, decision owner, and rollback/stop controls. Obtain Sean's explicit authorization. Without it, record BLOCKED and perform no enrollment or observation mutation.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0B-DAY0`; require green `prerequisites` for `M0B-COHORT-AUTHORITY` and `M0B-INSTRUMENTATION` plus green `accepted_targets` for exact `M0B-DAY0`. Before 01-38, remain BLOCKED and do not start observation.</verify>
  <done>Sean's authorization is authenticated and candidate-specific, or the plan remains blocked without side effects.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Seal the Day-0 start receipt</name>
  <action>Start the observation once, using current UTC time and real validated cohorts. Emit `M0B-DAY0.json` to the external evidence CAS with exact start/end bounds, build/tree, immutable usability-protocol digest, classification schema, environment, signer, and predecessor receipt identities. Reject simulated, backdated, or backfilled events.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0B-DAY0`; require target `M0B-DAY0` in green `accepted_targets` for the exact candidate/protocol/window. A top-level green result without that target is failure.</verify>
  <done>A single authenticated Day-0 receipt exists externally and does not claim completion or M0B acceptance.</done>
</task>
</tasks>

<threat_model>
STRIDE: candidate and signer identities resist spoofing; timestamps and policy digests prevent tampering; authorization is non-repudiable; no content leaves the aggregate boundary; observation secrets are external; replay/backfill cannot elevate an unobserved cohort.
</threat_model>

<verification>
External target-verifying gate only after 01-38; the current prerequisite-only command, source changes, and local receipts cannot substitute.
</verification>

<success_criteria>
Real observation start has explicit authorization and an authenticated Day-0 receipt, without any completion claim.
</success_criteria>

<output>
Create `01-03-SUMMARY.md` only after externally verifying the start receipt; otherwise record the exact blocker.
</output>
