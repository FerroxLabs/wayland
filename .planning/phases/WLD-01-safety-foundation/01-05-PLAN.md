---
phase: WLD-01-safety-foundation
plan: 05
type: execute
wave: 10
depends_on: ['01-04', '01-38']
files_modified: []
autonomous: false
requirements: [SAF-02, SAF-05]
must_haves:
  truths:
    - 'M0B acceptance is a distinct signed decision, not inferred from elapsed evidence.'
    - 'Thresholds, denominators, usability protocol, stop conditions, and owner are explicit.'
    - 'Failed thresholds produce a signed non-acceptance decision.'
  artifacts:
    - path: external-evidence/M0B.json
      provides: signed cohort-acceptance decision
  key_links:
    - from: external-evidence/M0B.json
      to: external-evidence/M0B-OBSERVATION-COMPLETE.json
      via: authenticated predecessor digest
---

<objective>
Produce the independently signed M0B decision after observation completion.

Terminal claim: the named decision owner signed a threshold-complete M0B decision bound to the exact completion receipt and product candidate.
</objective>

<execution_context>
Follow D-02, D-03, D-06, D-12, and D-13. Passing M0B does not merge, release, deploy, or promote traffic.
</execution_context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review protocol and threshold results</name>
  <action>Present aggregate results for every script, segment, rubric item, novice quick/substantial start, expert interaction budget, disclosure persistence, authority comprehension, crashes, support, accessibility, return reason, Classic comparison, soak/sample minimum, and zero-tolerance category from the immutable usability-protocol digest. Require every predeclared threshold and denominator; do not reinterpret protocol after seeing results.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0B-OBSERVATION`; require exact `M0B-OBSERVATION-COMPLETE` in green `accepted_targets` before reviewing the decision.</verify>
  <done>Every criterion has an evidence-backed decision and any failed criterion keeps invited alpha disabled.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Sign and register M0B</name>
  <action>The named decision owner signs `M0B.json` containing exact candidate/tree, immutable usability-protocol digest, Day-0 and completion digests, threshold decisions, environment, signer, and outcome. Register it in the external accepted-packet registry only on PASS; register a non-acceptance decision separately on FAIL.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0B`; require green prerequisite `M0B-OBSERVATION-COMPLETE` and exact `M0B` in green `accepted_targets`. Do not use the aggregate gate to accept this bounded decision.</verify>
  <done>An independently signed decision exists; only authenticated PASS can satisfy the M0B dependency.</done>
</task>
</tasks>

<threat_model>
STRIDE: signer and candidate identity prevent spoofing; digest chain prevents tampering; signed criterion decisions prevent repudiation; only aggregates are disclosed; signing failure is safe; no result can self-elevate into rollout authority.
</threat_model>

<verification>
Use the external accepted-packet registry and evidence CAS. A local `M0B.json` is non-authoritative.
</verification>

<success_criteria>
M0B has a distinct authenticated decision whose PASS/FAIL follows the predeclared protocol exactly.
</success_criteria>

<output>
Create `01-05-SUMMARY.md` citing only sanitized result and digests.
</output>
