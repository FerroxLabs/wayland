---
phase: WLD-01-safety-foundation
plan: 24
type: execute
wave: 3
depends_on: ['01-13']
files_modified:
  - src/process/bridge/officecliInstaller.ts
  - src/process/bridge/pptPreviewBridge.ts
  - src/process/bridge/officeWatchBridge.ts
  - tests/unit/officecliInstaller.test.ts
  - tests/unit/pptPreviewBridge.test.ts
  - tests/unit/officeWatchBridge.test.ts
  - tests/unit/coworkAuthorityIsolation.test.ts
autonomous: true
requirements: [COW-01, SAF-05]
must_haves:
  truths:
    - 'Cowork preserves caller authority and cannot reach a hosted fallback.'
    - 'A represented future alternative remains unavailable and confers no present consent or execution claim.'
  artifacts:
    - path: src/process/bridge/officecliInstaller.ts
      provides: fail-closed current-host OfficeCLI authority boundary
  key_links:
    - from: src/process/bridge/officecliInstaller.ts
      to: contracts/officecli/v1/contract.json
      via: accepted lockstep capability from 01-13
---

<objective>
Apply the accepted OfficeCLI lockstep contract to Cowork authority and fallback behavior.

Terminal claim: Cowork can invoke only the accepted current-host OfficeCLI capability with unchanged caller authority; no hosted fallback is reachable, and any represented future alternative is unavailable.
</objective>

<execution_context>
This is the sequential authority successor to 01-13's supply-chain contract. It makes no current consent-flow claim because no fallback execution exists.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce current-host-only authority isolation</name>
  <files>src/process/bridge/officecliInstaller.ts, src/process/bridge/pptPreviewBridge.ts, src/process/bridge/officeWatchBridge.ts, tests/unit/officecliInstaller.test.ts, tests/unit/pptPreviewBridge.test.ts, tests/unit/officeWatchBridge.test.ts, tests/unit/coworkAuthorityIsolation.test.ts</files>
  <action>Consume only the accepted lockstep capability and its exact verified executable path. Preserve caller `ask` or `trusted-edits` authority and reject bare-PATH `officecli`, injected paths, npm/global bootstrap, executable replacement, hidden network/cost paths, privilege widening, stale alternative state, and any hosted fallback execution. Bind both PPT preview and Office watch bridges to the exact capability path and fail closed when it is unavailable or changes. If a future alternative is represented, expose only unavailable metadata; it cannot mint consent, authority, readiness, or execution. Reuse existing user-visible text and locale keys; this plan owns no locale/generated files.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/officecliInstaller.test.ts tests/unit/pptPreviewBridge.test.ts tests/unit/officeWatchBridge.test.ts tests/unit/coworkAuthorityIsolation.test.ts && bun run typecheck</verify>
  <done>Cowork remains an intent over the ordinary canonical run and cannot reroute or elevate.</done>
</task>
</tasks>

<threat_model>
Contract/candidate identity blocks spoofing; path and capability checks detect tampering; accepted receipts support attribution; documents stay outside receipts; missing capability denies execution; Cowork cannot elevate authority.
</threat_model>

<verification>
Run `bun run lint -- src/process/bridge/officecliInstaller.ts src/process/bridge/pptPreviewBridge.ts src/process/bridge/officeWatchBridge.ts tests/unit/officecliInstaller.test.ts tests/unit/pptPreviewBridge.test.ts tests/unit/officeWatchBridge.test.ts tests/unit/coworkAuthorityIsolation.test.ts`, focused tests, typecheck, and exact full `bun run test` before summary or commit. Plan 01-31 owns external C0-A target acceptance; this construction plan invokes no acceptance gate.
</verification>

<success_criteria>
C0-A is bounded to exact current-host OfficeCLI with no reachable fallback or implied current consent.
</success_criteria>

<output>
Create `01-24-SUMMARY.md` only after the full local gate, separating construction from external C0-A acceptance.
</output>
