---
phase: WLD-01-safety-foundation
plan: 20
type: execute
wave: 7
depends_on: ['01-19', '01-22']
files_modified:
  - contracts/recovery/historical-transactions
  - scripts/recovery/verifyHistoricalTransactionCorpus.mjs
  - tests/unit/scripts/recovery/verifyHistoricalTransactionCorpus.test.ts
autonomous: true
requirements: [SAF-01, SAF-03, SAF-05]
must_haves:
  truths:
    - 'Historical transactions come from `src/process/services/constitution/constitutionFsTransaction.ts` at exact producer commit `991c502e74506ec3702f92e429a8b31b655412ba`, not reconstructed happy paths.'
    - 'The harness rejects incomplete, reordered, cross-release, or source-ambiguous transactions.'
  artifacts:
    - path: contracts/recovery/historical-transactions/manifest.json
      provides: complete corpus inventory and provenance digests
  key_links:
    - from: contracts/recovery/historical-transactions
      to: scripts/recovery/verifyHistoricalTransactionCorpus.mjs
      via: whole-directory inventory and transaction-chain replay
---

<objective>
Seal the exact historical transaction corpus and provenance harness required before M0A.

Terminal claim: every declared historical restore, downgrade, rollback, and re-upgrade transaction authenticates its source release, ordered mutations, expected state delta, and terminal outcome through one complete corpus manifest.
</objective>

<execution_context>
Own the entire `contracts/recovery/historical-transactions` directory and its inventory. Captured/redacted historical evidence must retain origin and digest; handcrafted fixtures must be marked synthetic and cannot substitute for required captured provenance.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build and hostile-replay the historical corpus</name>
  <files>contracts/recovery/historical-transactions, scripts/recovery/verifyHistoricalTransactionCorpus.mjs, tests/unit/scripts/recovery/verifyHistoricalTransactionCorpus.test.ts</files>
  <action>Generate retained bytes only from `src/process/services/constitution/constitutionFsTransaction.ts` in the isolated exact producer tree `991c502e74506ec3702f92e429a8b31b655412ba`. The manifest binds producer commit/tree/archive, exact source-path Git blob and transaction-region before/after digest, compiler/toolchain, generator command/version, helper digest, every fixture digest, and real crash points including committed, after ledger publication, and before journal/receipt completion. Store any additive hook-invocation patch verbatim as `harness-only.patch` with a separate digest and source-trace proof that transaction, serialization, format, and durability regions are byte-identical. Store deterministic manifest-finalizer inputs/commands separately. Replay the real pending/crash states and reject current-code reconstruction, synthetic-as-captured claims, missing/extra files, gaps, conflicts, reordering, cross-release substitution, unknown critical fields, state drift, or post-terminal events. No new locale/generated files are owned.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/scripts/recovery/verifyHistoricalTransactionCorpus.test.ts</verify>
  <done>The corpus reproduces from the nominated historical source and real crash hooks, the harness patch is transaction-neutral, and every hostile transaction fails closed.</done>
</task>
</tasks>

<threat_model>
Source/release identity blocks spoofing; whole-directory digests detect tampering; ordered transactions prevent repudiation; redaction avoids user-content disclosure; incomplete history cannot mint acceptance.
</threat_model>

<verification>
Run `bun run lint -- scripts/recovery/verifyHistoricalTransactionCorpus.mjs tests/unit/scripts/recovery/verifyHistoricalTransactionCorpus.test.ts`, the focused suite, and exact full `bun run test` before summary or commit.
</verification>

<success_criteria>
M0A consumes provenance-backed historical behavior rather than implementation-authored assumptions.
</success_criteria>

<output>
Create `01-20-SUMMARY.md` only after the full gate, with corpus inventory/digest and provenance classification counts.
</output>
