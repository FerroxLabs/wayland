---
phase: WLD-01-safety-foundation
plan: 08
type: execute
wave: 2
depends_on: ["01-40"]
files_modified:
  - src/process/services/workspaceRetention.ts
  - src/process/bridge/workspaceRetentionBridge.ts
  - tests/unit/workspaceRetention.test.ts
  - tests/unit/process/bridge/workspaceRetentionBridge.test.ts
  - tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts
autonomous: true
requirements: [SAF-04]
must_haves:
  truths:
    - "Chat deletion preserves every referenced, scheduled, artifact-bearing, modified, or unknown workspace."
    - "Only proven empty abandoned shells can be classified for human review."
    - "Phase 1 has no quarantine, prune, or delete authority."
  artifacts:
    - path: src/process/services/workspaceRetention.ts
      provides: preservation-first classification
    - path: src/process/bridge/workspaceRetentionBridge.ts
      provides: read-only review projection
  key_links:
    - from: src/process/bridge/workspaceRetentionBridge.ts
      to: src/process/services/workspaceRetention.ts
      via: main-owned classifier with no mutation IPC
---

<objective>
Make managed workspace retention preservation-first and classification-only.

Terminal claim: deleting a chat cannot remove or quarantine user work, while provably empty abandoned shells are classified for later human review.
</objective>

<execution_context>
D-07 is the authority ceiling. Do not implement the deferred managed-workspace lifecycle.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Make classification exhaustive and conservative</name>
  <files>src/process/services/workspaceRetention.ts, tests/unit/workspaceRetention.test.ts</files>
  <action>Classify referenced, scheduled, artifact-bearing, modified, active, empty-abandoned, and unknown workspaces from process-owned evidence. Default missing, malformed, raced, unreadable, or contradictory evidence to preserve/unknown. Remove any Phase-1 mutation path and prove chat deletion only severs the chat reference.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/workspaceRetention.test.ts</verify>
  <done>Hostile and race cases preserve content; only exact empty-abandoned evidence yields review classification.</done>
</task>
<task type="auto">
  <name>Task 2: Restrict the bridge to read-only review</name>
  <files>src/process/bridge/workspaceRetentionBridge.ts, tests/unit/process/bridge/workspaceRetentionBridge.test.ts, tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts</files>
  <action>Expose classification and explanation only. Reject path injection, arbitrary roots, renderer-supplied classifications, mutation verbs, and unknown IPC fields. Ensure no quarantine/delete/prune capability is registered or returned.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/process/bridge/workspaceRetentionBridge.test.ts tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts && bun run typecheck</verify>
  <done>Renderer can inspect conservative classifications but cannot mutate workspace content or authority.</done>
</task>
</tasks>

<threat_model>
STRIDE: renderer classifications and paths cannot spoof authority; filesystem races fail preserve; decisions are explainable; paths/content are not over-disclosed; unreadable state is safe; no IPC grants destructive privilege.
</threat_model>

<verification>
Run focused tests, `bun run lint -- src tests`, and exact full `bun run test` before summary or commit; inspect source for all delete/quarantine/prune verbs in this bridge.
</verification>

<success_criteria>
All uncertain work is preserved and the only positive classification is a non-destructive review candidate.
</success_criteria>

<output>
Create `01-08-SUMMARY.md` only after the full gate and explicitly state that lifecycle mutation is not delivered.
</output>
