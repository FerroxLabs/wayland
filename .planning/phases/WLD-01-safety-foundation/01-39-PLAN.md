---
phase: WLD-01-safety-foundation
plan: 39
type: execute
wave: 9
depends_on: ['01-18', '01-31', '01-38']
files_modified: []
autonomous: false
requirements: [SAF-01, SAF-03, SAF-04, SAF-05]
must_haves:
  truths:
    - 'All six declared target cells execute the complete recovery journey.'
    - 'Core-bearing cells require accepted producer quiescence issue #896.'
    - 'Only the schema-v2 target result can authenticate M0A.'
  artifacts:
    - path: external-evidence/M0A.json
      provides: authenticated complete target-matrix acceptance
  key_links:
    - from: 01-18 immutable harness
      to: external-evidence/M0A.json
      via: exact package/environment/target receipts and schema-v2 target verification
---

<objective>
Execute and authenticate the complete six-target M0A recovery matrix after its local harness and authority prerequisites pass.

Terminal claim: all target cells prove install, preserve, inspect, restore, signed rollback, isolated boot, and delta-safe re-upgrade for one exact candidate, and schema-v2 target verification authenticates M0A.
</objective>

<execution_context>
D-02, D-07, D-12, and D-13 apply. Accepted Core quiescence #896 is a hard predecessor for every Core-bearing cell. Plan 01-18 source presence, local macOS proof, or package presence cannot substitute for the matrix.
</execution_context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute and accept the signed target matrix</name>
  <action>Run the 01-18 harness externally for macOS arm64/x64, Windows arm64/x64, and Linux arm64/x64 using copied/disposable representative state. Capture install, backup, inspect, restore, signed rollback, boot, re-upgrade, preserved-state, authority, and rollback-drill receipts. Authenticate `M0A.json` only when all cells pass. Missing #896 or any target/publisher evidence keeps exact cells and M0A red.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-M0A`; require exact `M0A` in green `accepted_targets`. An empty or prerequisite-only result fails.</verify>
  <done>Complete signed six-target evidence and target authentication pass, or M0A remains blocked with exact cell failures and no inferred acceptance.</done>
</task>
</tasks>

<threat_model>
STRIDE: target/package/signer identity blocks spoofing; cell/state digests detect tampering; signed journeys prevent repudiation; user content stays in disposable fixtures; missing targets fail safely; no partial or earlier evidence state elevates M0A.
</threat_model>

<verification>
Use only the installed schema-v2 verifier proven by 01-38 and the exact 01-18 harness commit. No repository-local receipt substitutes.
</verification>

<success_criteria>
M0A exists only from complete accepted six-target recovery evidence with Core quiescence where required.
</success_criteria>

<output>
Create `01-39-SUMMARY.md` with sanitized matrix status, target result, receipt IDs, and any failing cells.
</output>
