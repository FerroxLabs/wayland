---
phase: WLD-01-safety-foundation
plan: 38
type: execute
wave: 3
depends_on: ["01-37", "01-40"]
files_modified:
  - /Users/seandonahoe/.local/bin/wayland-gsd-gate
  - /Users/seandonahoe/.local/lib/wayland-gsd/packet-gate-lib.mjs
  - /Users/seandonahoe/.config/wayland-gsd/desktop-control.json
  - /Users/seandonahoe/.local/share/wayland-gsd/verifier-backups
autonomous: true
requirements: [SAF-05]
must_haves:
  truths:
    - "Installed verifier bytes match the exact committed 01-37 source and digest."
    - "External control pins the successor commit, controlled paths, and verifier digest."
    - "No acceptance key or packet authorization is provisioned by this plan."
  artifacts:
    - path: /Users/seandonahoe/.local/lib/wayland-gsd/packet-gate-lib.mjs
      provides: installed digest-pinned schema-v2 verifier library
    - path: /Users/seandonahoe/.config/wayland-gsd/desktop-control.json
      provides: external successor control commit and verifier digest pin
  key_links:
    - from: 01-37 exact committed source
      to: installed wrapper/library/control
      via: byte comparison, SHA-256 digest, control-commit ancestry, and fail-closed smoke
---

<objective>
Install and externally pin the exact schema-v2 verifier source before any target-acceptance plan runs.

Terminal claim: the installed wrapper, verifier library, and external control config are bound to the exact tested 01-37 commit and reject a missing target without any acceptance key.
</objective>

<execution_context>
This is a reversible local control-plane installation explicitly authorized as part of the Desktop build migration. It may copy only exact committed 01-37 verifier bytes and update external pins. It must not create or rotate a private/public acceptance key, authorize a candidate, add an accepted packet, merge, release, deploy, or change any existing key/accepted-packet registry value.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Install exact committed verifier bytes and successor pins</name>
  <action>After 01-37 is committed and its full proof is green, assert the expected worktree, branch, clean exact HEAD, commit/tree, and file digests. Before writing, copy the installed wrapper, library, and control JSON into a timestamped mode-preserving backup directory. Capture canonical digests of `keys` and `accepted_packets`. Atomically copy the exact committed wrapper/library bytes into the installed locations and atomically update only the successor control commit, controlled paths, schema revision, and installed-library digest. Preserve `keys` and `accepted_packets` byte-for-byte or canonical-value-for-value. Do not install from a dirty worktree or manually reconstructed source. Any failed copy, pin, comparison, or smoke test automatically restores the complete backup before returning red.</action>
  <verify>From a clean checkout at the 01-37 commit, run `git diff --exit-code &lt;01-37-commit&gt; -- .planning/execution`, compare repository and installed wrapper/library bytes with `cmp`, compare `shasum -a 256` output to `verifier_lib_digest`, and compare pre/post canonical digests of `keys` and `accepted_packets`. Every mismatch restores the backup and exits nonzero.</verify>
  <done>Installed bytes and external pins match the exact tested commit, existing authority registries are unchanged, and an exercised local rollback exists.</done>
</task>
<task type="auto">
  <name>Task 2: Prove installed target enforcement without minting acceptance</name>
  <action>Independently inspect the installed wrapper, imported library, trust config, controlled commit, and live output after the atomic install. Execute a schema-v2 acceptance gate without adding any key or accepted packet. It must fail because its declared target is not externally authorized and must return a distinct red `accepted_targets` result; empty-target or prerequisite-only success is a blocker. Re-run the repository hostile suite at the pinned commit. On any failure, restore the pre-install backup and re-run the prior verifier smoke test so rollback itself has evidence.</action>
  <verify>From the clean pinned commit, run `wayland-gsd-gate P1-C0A`; require nonzero exit, `prerequisites` separate from `accepted_targets`, and target `C0-A` rejected as not externally authorized. Then run `node --test .planning/execution/*.test.mjs &amp;&amp; bun run lint -- .planning/execution &amp;&amp; bun run test`. Do not proceed if output retains v1 `required`/`alternatives` only, the missing-target gate exits zero, or any repository proof fails.</verify>
  <done>The installed successor is exact, independently pinned, and demonstrably fail-closed on its missing target without changing acceptance authority; failed installation automatically rolls back.</done>
</task>
</tasks>

<threat_model>
STRIDE: commit/tree and byte identity prevent installer spoofing; external digests detect tampering; independent verification prevents repudiation; config output is sanitized; failed copy/pin is non-destructive; installation cannot elevate itself into packet acceptance.
</threat_model>

<verification>
Exact committed source, prewrite backup, atomic installation, byte comparison, SHA-256 pin, unchanged authority-registry digests, external control inspection, live missing-target failure, `node --test .planning/execution/*.test.mjs`, `bun run lint -- .planning/execution`, exact full `bun run test`, and rollback-on-failure proof are all required. Source presence alone is insufficient.
</verification>

<success_criteria>
The installed schema-v2 verifier is pinned and fail-closed, while acceptance keys and packet authorizations remain separately unprovisioned.
</success_criteria>

<output>
Create `01-38-SUMMARY.md` with only sanitized commit/tree/digests and live fail-closed result. Never include key material.
</output>
