---
phase: WLD-01-safety-foundation
plan: 17
type: execute
wave: 10
depends_on: ['01-39', '01-28', '01-38']
files_modified: []
autonomous: false
requirements: [FLUX-01, SAF-05]
must_haves:
  truths:
    - 'Exactly one mutually exclusive Flux capability outcome is accepted.'
    - 'Live M1F requires trusted delivery and authoritative charge correlation.'
    - 'NO-FLUX-CLAIMS requires physical absence of all dependent claims.'
  artifacts:
    - path: external-evidence/M1F.json
      provides: trusted live Flux delivery acceptance
    - path: external-evidence/NO-FLUX-CLAIMS.json
      provides: signed physical-absence alternative
  key_links:
    - from: external-evidence/M1F.json|NO-FLUX-CLAIMS.json
      to: external accepted-packet registry
      via: mutually exclusive contract branch
---

<objective>
Close the Flux Phase-1 gate through either trusted live M1F or the signed physical-absence alternative.

Terminal claim: the accepted registry contains exactly one of authenticated live M1F or authenticated NO-FLUX-CLAIMS for the exact candidate.
</objective>

<execution_context>
D-09 and D-12 apply. The schema-v2 gate produced by 01-37 and installed by 01-38 enforces one accepted target from `M1F` and `NO-FLUX-CLAIMS`; both-present and neither-present states fail closed. A degraded runtime is construction-only until the physical-absence receipt authenticates.
</execution_context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prove and authenticate exactly one Flux capability branch</name>
  <action>Evaluate both branches against the same exact candidate. If a trusted Flux endpoint/stream exists, prove current capability response plus full request-attempt-retry-fallback-response-ledger-debit correlation and observed provider/Flux usage/cost; authenticate `M1F.json` only on complete live proof. Otherwise inspect exact source, build, package, runtime projections, and release claims and prove route explanations, provider attempts, Flux costs, reconciliation, and debit claims are physically absent; authenticate `NO-FLUX-CLAIMS.json` only on complete absence proof. Reject missing credentials as evidence of either branch, partial transport, estimates, ACP cumulative cost, catalog price, stale/candidate claims, hidden/disabled rather than absent surfaces, both branches, and neither branch.</action>
  <verify>After 01-38 has installed and pinned the exact schema-v2 verifier, run `wayland-gsd-gate P1-M1F`; require green `prerequisites` for `M0A` and `FLUX-PRODUCER-ACCEPTANCE` plus exactly one green `accepted_targets` branch from `M1F` and `NO-FLUX-CLAIMS`. Confirm the other target is absent/red. Both or neither fail.</verify>
  <done>Exactly one branch is accepted for the exact candidate; neither fixture-only corpus nor degraded runtime alone opens the gate.</done>
</task>
</tasks>

<threat_model>
STRIDE: producer/candidate identity blocks spoofing; transport and package scans detect tampering; signed branch choice prevents repudiation; customer/account details remain out of receipts; absent delivery fails safely; no estimate or hidden UI elevates accounting authority.
</threat_model>

<verification>
Use external accepted-packet registry with a mechanical XOR constraint.
</verification>

<success_criteria>
Flux truth closes through one evidence-backed branch and never through degraded construction alone.
</success_criteria>

<output>
Create `01-17-SUMMARY.md` naming the accepted branch and exact receipt digest.
</output>
