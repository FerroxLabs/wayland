---
phase: WLD-01-safety-foundation
plan: 14
type: execute
wave: 11
depends_on: ["01-05", "01-08", "01-17", "01-39", "01-27", "01-29", "01-30", "01-31", "01-38"]
files_modified: []
autonomous: false
requirements: [SAF-01, SAF-02, SAF-03, SAF-04, SAF-05, CORE-01, CORE-02, FLUX-01, MCP-01, SBX-01, COW-01]
must_haves:
  truths:
    - "Every Phase-1 packet is authenticated for the exact landed commit/tree and external gate revision."
    - "Implementation, fixture, package, deployment, canary, release, and cohort states remain separate."
    - "Phase 1 remains open until signed M0B and Phase-5 exact-package closure are both accepted."
  artifacts:
    - path: external-evidence/PHASE1-AGGREGATE-ACCEPTANCE.json
      provides: authenticated aggregate sentinel
  key_links:
    - from: external-evidence/PHASE1-AGGREGATE-ACCEPTANCE.json
      to: external accepted-packet registry
      via: exact receipt DAG including PHASE5-PROOF-CLOSURE
---

<objective>
Authenticate the complete Phase-1 receipt DAG and close the aggregate sentinel only when every engineering, observation, and exact-package dependency exists.

Terminal claim: `PHASE1-AGGREGATE-ACCEPTANCE` is signed for one exact integrated candidate after all named receipts and the Phase-5 proof closure authenticate, with zero unresolved in-scope findings.
</objective>

<execution_context>
D-02, D-03, D-11, D-12, and D-13 govern. Verify against gate revision `wld-packet-gates-2026-07-19.6`, including its exact-one Flux capability rule. This plan is intentionally blocked until the cross-phase `PHASE5-PROOF-CLOSURE` receipt exists; that blocker is correct state, not permission to fake closure.
</execution_context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Authenticate the receipt DAG and exact candidate</name>
  <action>Using the installed external verifier, require accepted M0A (including accepted Core quiescence #896 and signed six-target recovery matrix), signed M0B, workspace preservation, Core, exactly one of live M1F or NO-FLUX-CLAIMS, MCP-0, SBX-0, C0-A, and `PHASE5-PROOF-CLOSURE`. Match exact landed commit/tree, gate revision, contracts, signers, environments, evidence digests, producer pins, and control-plane bytes. Reject stale, local-only, sibling-commit, unsigned, expired, or partially accepted receipts.</action>
  <verify>After accepted 01-38, run `wayland-gsd-gate P1-AGGREGATE-ACCEPTANCE`; require every declared predecessor in green `prerequisites` and exact `PHASE1-AGGREGATE-ACCEPTANCE` in green `accepted_targets` for one candidate. Before 01-38, Phase 1 remains open.</verify>
  <done>Every predecessor is authenticated for the same exact candidate and no state substitution occurred.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run aggregate attack and seal acceptance</name>
  <action>Run full tests, lint, typecheck, security scan, build, exact-package replay, deployment proof, rollback drill evidence, and independent adversarial audit against the exact integrated head. Resolve all Critical/High and all in-scope Medium/Low defects rather than hiding them in status. Emit the aggregate receipt only at zero unresolved in-scope findings and after explicit Sean authorization for any Tier-2 action.</action>
  <verify>After accepted 01-38, rerun `wayland-gsd-gate P1-AGGREGATE-ACCEPTANCE`; require the same exact target in green `accepted_targets` after the zero-finding proof. A green prerequisite DAG without the target is failure. Tier-2 actions remain separate.</verify>
  <done>`PHASE1-AGGREGATE-ACCEPTANCE` exists externally or the phase stays open with exact blockers and no false completion.</done>
</task>
</tasks>

<threat_model>
STRIDE: exact commit/tree/signer identity blocks spoofing; receipt DAG and CAS detect tampering; signed audit/authorization prevents repudiation; sanitized receipts prevent disclosure; missing gates fail safely; no earlier evidence state can elevate into release or cohort authority.
</threat_model>

<verification>
Authenticate the exact external DAG and current integration candidate. Do not create substitute repository-local evidence.
</verification>

<success_criteria>
Phase 1 closes only through the authenticated aggregate sentinel after real M0B and Phase-5 exact-package proof; otherwise it truthfully remains in progress.
</success_criteria>

<output>
Create `01-14-SUMMARY.md` only with sanitized aggregate status and exact receipt/blocker IDs.
</output>
