---
phase: WLD-D-inbox-repairs / D-05
verified: 2026-07-24T03:59:03Z
status: human_needed
score: 6/6 must-haves verified (code-provable), 1 packaged live-verify pending
behavior_unverified: 1
overrides_applied: 0
github_issue: 853
behavior_unverified_items:
  - truth: 'In the PACKAGED app, an exec/process failure (renamed / chmod 000 / AV-blocked wcore binary, or an OS SIGKILL) surfaces its real errno or kill signal in the chat error plus a reachable logs path that holds the detail, while a normal provider/model API error still renders verbatim'
    test: 'bun run package (NEVER raw electron-vite build; then revert the regenerated src/process/services/constitution/constitutionFsAuthority.generated.ts). In the running packaged app rename or `chmod 000` the bundled wayland-core binary (or block it via Gatekeeper/AV) and send a message. Confirm the chat error names the real reason (an ENOENT/EACCES errno, or a kill signal if the OS SIGKILLs it) and shows a working logs path; open the electron-log at that path and confirm the reason is present. Separately confirm a normal provider/model API error still renders verbatim.'
    expected: "Broken build = real errno or signal + a reachable log holding the detail (no generic text, no 'code null', no main-process crash). Provider/API error path unchanged."
    why_human: "The #853 symptom manifests in the shipped/packaged app against the real bundled binary, real Gatekeeper/AV behavior, and the real app.getPath('logs') location. Unit tests prove the producer composition + surfacing end-to-end with the real helper code path, but the packaged-artifact acceptance is the Milestone D acceptance and is run by hand by the orchestrator + Sean. Do NOT build the packaged app in this verification pass."
human_verification:
  - test: 'Packaged live-verify — bun run package, rename/chmod 000 the bundled wayland-core binary, send a message; confirm the chat error names the errno or kill signal and a reachable logs path (the reason is in the log at that path). Confirm a normal provider/model API error still renders verbatim.'
    expected: "Blocked binary yields a real errno/signal + reachable log (not generic text, not 'code null', not a crash); provider error still verbatim."
    why_human: 'Packaged-artifact acceptance against the real bundled binary + real OS block + real logs dir; the single remaining Milestone D acceptance step.'
---

# Phase D-05: Surface real exec/process-failure reason + reachable log link (#853) — Verification Report

**Phase Goal:** When wayland-core (or its child) fails to spawn/exec or is killed, the user sees the REAL reason (errno ENOENT/EACCES/EPERM, or the kill signal like SIGKILL) plus a reachable log path — instead of generic text or "code null" — with no secret leaks and no new crash paths. Provider API errors stay verbatim/untouched.
**Verified:** 2026-07-24T03:59:03Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Verdict

**GOAL MET** at the level unit-verification + static analysis can prove, with the packaged GUI live-verify (rename/chmod the wcore binary → real errno/signal + reachable log) as the single named remaining Milestone D acceptance (expected, not a gap).

Every one of the 6 goal-backward points is demonstrably true in the code and locked by a passing test. The fix is exactly the LOCKED exec-only boundary the plan promised: one new pure module (`execFailureReason.ts`) + three narrow production edits (a synchronously-attached child `on('error')` handler, `signal` capture on exit, a redacted log-link append), zero touch to the provider stream `type:'error'` path, the Desktop-contract fail-closed path (`index.ts:620-637`), or the #484 stderr enrichment (which is extended, not rewritten). Tests were authored first (commit `d0236f079`) and the fix commit (`50b16a91c`) flips every new assertion green while the pre-existing byte-exact code assertions stay green.

I ran the four named test files (67 tests, all pass) and `tsc --noEmit -p tsconfig.json` (exit 0, clean) myself. The full suite (executor-reported 15,645/1, the 1 being the known WorkflowDetailModal parallelism flake that passes isolated) is reported-not-reverified per scope; the WorkflowDetailModal flake is out of scope.

**Verification note:** live HEAD is `2a1fec79f` (a later D-04 commit). That commit touches only `IjfwSetupStatus.tsx` / `IjfwSetupStatus.dom.test.tsx` — no D-05 file — so the working-tree state of all D-05 files is byte-identical to the D-05 final commit `50b16a91c`. Verification was performed against that current state.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                    | Status                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Spawn errno (ENOENT/EACCES/EPERM) surfaces the real errno to the user (not just logged), and the new `on('error')` handler prevents the unhandled-'error' main crash — attached synchronously with spawn | ✓ VERIFIED                     | New `spawnedChild.on('error', ...)` at `index.ts:705-712` composes `redactSecrets(describeSpawnError(err))`, rejects `readyReject` when `!this.ready` else emits `type:'error'`. Spawn is at `index.ts:586` (`this.childProcess = spawn(...)`); handler at `705`. Grep for `await` between 586 and 705 = none → handler attaches in the same synchronous turn as spawn, so no spawn 'error' can arrive before the listener. `describeSpawnError` maps each errno to a missing/blocked lead + raw token (`execFailureReason.ts:29-43`). Proven by `wcoreExecFailureReason` tests 1-3 (composition) + `WCoreManagerStartFailure` errno test (surfaced end-to-end, `/test/logs` present).                                                                                                                                                  |
| 2   | A SIGKILL surfaces "killed by SIGKILL" (equiv.), NOT "code null", through BOTH the init-reject and mid-turn `handleProcessExit`                                                                          | ✓ VERIFIED                     | `on('exit', (code, signal) => ...)` now captures signal (`index.ts:713`). Init reject composes `const reason = describeExitReason(code, signal)` → `wcore ${reason} during init` (`index.ts` reject block). Signal forwarded via `this._onProcessExit(code, this.activeMsgId, signal)`. Manager `handleProcessExit(code, activeMsgId, signal?)` composes `Agent process ${describeExitReason(code, signal ?? null)}` (`WCoreManager.ts:~1207`); wiring `onProcessExit: (code, activeMsgId, signal) => this.handleProcessExit(...)` (`WCoreManager.ts:573`). `describeExitReason(null,'SIGKILL')` = "killed by SIGKILL (likely antivirus, firewall, or OS code-signature block)". Proven by `wcoreExecFailureReason` test 4 (`not.toContain('code null')`) + `WCoreManagerProcessExit` SIGKILL test (contains SIGKILL, not 'code null'). |
| 3   | Numeric exit unchanged (W1): `describeExitReason(N, null)` is byte-exact `exited with code N`; `wcoreStderrSurfacing` exact `.toBe` on codes 0/1/127 still pass and were NOT weakened                    | ✓ VERIFIED                     | `describeExitReason` returns `exited with code ${code}` when no signal (`execFailureReason.ts:52-57`). `wcoreExecFailureReason` test asserts byte-exact `.toBe('exited with code 1'/'0'/'127')` (lines 62-64). `wcoreStderrSurfacing.test.ts:144` `.toBe('wcore exited with code 127 during init')` intact; the only diff to that file changed `child.emit('exit', 1)` → `emit('exit', 1, null)` and `toHaveBeenCalledWith(1,'active-turn')` → `(1,'active-turn', null)` — additive signal arg, assertions on wording NOT weakened.                                                                                                                                                                                                                                                                                                     |
| 4   | All newly-surfaced strings (errno, exit reason, log path) are redacted; the redaction test asserts an `sk-ant-` token is masked                                                                          | ✓ VERIFIED                     | Agent side: `redactSecrets(describeSpawnError(err))` (local `redactSecrets`, `index.ts:81`). Manager side: `redactCommandSecrets(...reason...${logLinkSuffix()})` on both `handleProcessExit` and `emitStartFailure`. `redactCommandSecrets` regex masks `sk-ant-` (`redactCommandSecrets.ts:40`). `WCoreManagerStartFailure` redaction test injects `sk-ant-abcdef0123456789ABCDEF` and asserts `not.toContain(token)` — passes.                                                                                                                                                                                                                                                                                                                                                                                                       |
| 5   | A reachable logs-dir path is appended (redacted) on the failure surfaces                                                                                                                                 | ✓ VERIFIED                     | `logLinkSuffix()` reads `getPlatformServices().paths.getLogsDir?.()` → `\n\nLogs: <dir>`, guarded (empty string, never throws). Appended in `handleProcessExit` and `emitStartFailure`, inside the `redactCommandSecrets` wrap. `getLogsDir` is a real `IPlatformServices` method (`IPlatformServices.ts:18`) resolving to `app.getPath('logs')` in Electron (`ElectronPlatformServices.ts:55-60`) → reachable in the packaged app. Tests assert `/test/logs` present on both surfaces.                                                                                                                                                                                                                                                                                                                                                 |
| 6   | Scope integrity: provider stream `type:'error'` rendering, the Desktop-contract fail-closed path (`index.ts:620-637`), and #484 stderr enrichment are byte-identical — no over-build                     | ✓ VERIFIED                     | The `50b16a91c` diff to `index.ts` touches only: the `describeSpawnError/describeExitReason` import, the widened `onProcessExit` option type, the new `on('error')` handler, the `(code, signal)` capture, and the init-reject `describeExitReason` composition (extends #484, preserving the `detail` stderr-tail append + "during init" framing and byte-exact "code N"). The `failDesktopContract` path (`index.ts:620-637`) and provider stream `type:'error'` handling are NOT in the diff. `WCoreManager.ts` diff touches only imports, the `onProcessExit` wiring, `logLinkSuffix`, `handleProcessExit`, `emitStartFailure`.                                                                                                                                                                                                     |
| 7   | Behavioral packaged-artifact proof of the exec-failure surface                                                                                                                                           | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code present + wired + unit-proven end-to-end with the real helper; the packaged-GUI live-verify (blocked binary → real errno/signal + reachable log) is the Milestone D acceptance, run by hand by orchestrator + Sean. Not built in this pass. See Human Verification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

**Score:** 6/6 truths verified (code-provable); 1 packaged live-verify pending (behavior_unverified).

### Required Artifacts

| Artifact                                       | Expected                                                                                                                        | Status     | Details                                                                                                                                                                  |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/process/agent/wcore/execFailureReason.ts` | New pure module: `describeSpawnError`, `describeExitReason`, no imports of index/manager, exec-only head comment                | ✓ VERIFIED | 57 lines; both functions pure + dependency-free; head comment states exec-only scope and that provider/API errors are elsewhere and redaction happens at callers.        |
| `src/process/agent/wcore/index.ts`             | Child `on('error')` handler (sync), `signal` capture on exit, widened `onProcessExit` type, `describeExitReason` in init reject | ✓ VERIFIED | +26/-10; all four edits present at the named sites; provider/Desktop-contract paths untouched.                                                                           |
| `src/process/task/WCoreManager.ts`             | `logLinkSuffix`, `describeExitReason` in `handleProcessExit`, `redactCommandSecrets` on both surfaces, signal forwarding        | ✓ VERIFIED | +31/-6; imports `getPlatformServices`, `redactCommandSecrets`, `describeExitReason` (distinct specifier from the mocked `@process/agent/wcore`); wiring forwards signal. |
| `tests/unit/wcoreExecFailureReason.test.ts`    | New pure composition tests (errno map, fallback, SIGKILL not-"code null", byte-exact codes)                                     | ✓ VERIFIED | 66 lines, 6 tests, all pass.                                                                                                                                             |
| `tests/unit/WCoreManagerStartFailure.test.ts`  | `getLogsDir` mock + errno-surfacing + redaction (`sk-ant-`) tests; 3 existing tests intact                                      | ✓ VERIFIED | +34/-1; new tests pass, existing pass.                                                                                                                                   |
| `tests/unit/WCoreManagerProcessExit.test.ts`   | `getLogsDir` mock + SIGKILL-surfacing (no "code null") test; existing tests intact                                              | ✓ VERIFIED | +13/-1; new test passes, existing pass.                                                                                                                                  |
| `tests/unit/wcoreStderrSurfacing.test.ts`      | Exit emits updated to `(code, null)` signal arg; byte-exact code assertions NOT weakened                                        | ✓ VERIFIED | +5/-3; only signal-arg additive change; `.toBe('...code 127...')` intact.                                                                                                |

### Key Link Verification

| From                                     | To                  | Via                                                                        | Status  | Details                                         |
| ---------------------------------------- | ------------------- | -------------------------------------------------------------------------- | ------- | ----------------------------------------------- |
| `index.ts` child `on('error')`           | user surface        | `redactSecrets(describeSpawnError(err))` → `readyReject` / `onStreamEvent` | ✓ WIRED | `index.ts:705-712`                              |
| `index.ts` `on('exit')`                  | manager             | `(code, signal)` → `_onProcessExit(code, activeMsgId, signal)`             | ✓ WIRED | signal captured + forwarded                     |
| `WCoreManager` `onProcessExit` wiring    | `handleProcessExit` | `(code, activeMsgId, signal)` passthrough                                  | ✓ WIRED | `WCoreManager.ts:573`                           |
| `handleProcessExit` / `emitStartFailure` | logs dir            | `logLinkSuffix()` → `getPlatformServices().paths.getLogsDir()`             | ✓ WIRED | real accessor `ElectronPlatformServices.ts:55`  |
| surfaced `data` strings                  | redaction           | `redactSecrets` (agent) / `redactCommandSecrets` (manager)                 | ✓ WIRED | `sk-ant-` masked (`redactCommandSecrets.ts:40`) |

### Behavioral Spot-Checks

| Behavior             | Command                                                                                                       | Result                                                                   | Status                                    |
| -------------------- | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------- |
| Four D-05 test files | `npx vitest run wcoreExecFailureReason WCoreManagerStartFailure WCoreManagerProcessExit wcoreStderrSurfacing` | 4 files, 67 tests passed / 0 failed                                      | ✓ PASS                                    |
| TypeScript typecheck | `npx tsc --noEmit -p tsconfig.json`                                                                           | exit 0, no errors                                                        | ✓ PASS                                    |
| Full unit suite      | (executor-reported)                                                                                           | 15,645 pass / 1 (WorkflowDetailModal parallelism flake, passes isolated) | ℹ️ reported-not-reverified (out of scope) |

### Anti-Patterns Found

| File              | Line | Pattern                | Severity | Impact                                                                                                                          |
| ----------------- | ---- | ---------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `WCoreManager.ts` | 1052 | `TODO(#422 follow-up)` | ℹ️ Info  | Pre-existing, references formal follow-up (#422), OUTSIDE every D-05 diff region — not introduced by this phase, not a blocker. |

No stubs, no empty implementations, no unreferenced debt markers in the D-05 changes. The `logLinkSuffix` defensive `catch { return '' }` degrades safely rather than throwing (correct — a platform without a logs dir yields no suffix, never a new crash path).

### Human Verification Required

**1. Packaged exec-failure live-verify (Milestone D acceptance)**

- **Test:** `bun run package` (NEVER raw `electron-vite build`; revert the regenerated `constitutionFsAuthority.generated.ts` after). In the running packaged app, rename or `chmod 000` the bundled `wayland-core` binary (or block via Gatekeeper/AV) and send a message. Then open a normal provider/model API error case.
- **Expected:** The chat error names the real reason — an `ENOENT`/`EACCES` errno, or a kill `signal` if the OS `SIGKILL`s it — and shows a working logs path; the electron-log at that path contains the reason. No generic text, no "code null", no main-process crash. The provider/model API error still renders verbatim (out-of-scope path untouched).
- **Why human:** Requires the shipped/packaged artifact against the real bundled binary, real OS block behavior, and the real `app.getPath('logs')` location. Do NOT build the packaged app in this verification pass — this is the orchestrator + Sean acceptance step.

### Gaps Summary

No code gaps. The goal is fully achieved at the level unit tests + static analysis can prove: an exec/process failure names its real errno or kill signal (never "code null"), carries a redacted reachable logs path, cannot leave the child `'error'` event unhandled (crash path closed), and leaves every out-of-scope surface (provider stream `type:'error'`, Desktop-contract fail-closed, #484 stderr enrichment) byte-identical. The only remaining item is the packaged-GUI live-verify by hand — the expected, planned Milestone D acceptance, not a defect.

---

_Verified: 2026-07-24T03:59:03Z_
_Verifier: Claude (ferrox-verifier)_
