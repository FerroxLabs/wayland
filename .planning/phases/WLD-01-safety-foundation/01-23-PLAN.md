---
phase: WLD-01-safety-foundation
plan: 23
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - contracts/cohort/m0b-usability-protocol.json
  - scripts/cohort/verifyM0BUsabilityProtocol.mjs
  - tests/unit/scripts/cohort/m0bUsabilityProtocol.test.ts
autonomous: true
requirements: [SAF-02, SAF-05]
must_haves:
  truths:
    - "Observation protocol is immutable before Day 0."
    - "Scripts, segments, rubric, thresholds, denominators, sample/soak minimums, stop rules, Classic comparison, and owner are complete."
    - "Every shared closed runtime enum and fixed protocol constant, including `M0B_DAY_MS = 86_400_000`, is mechanically equal to the frozen protocol; no copied subset can drift silently."
  artifacts:
    - path: contracts/cohort/m0b-usability-protocol.json
      provides: immutable M0B usability protocol
  key_links:
    - from: scripts/cohort/verifyM0BUsabilityProtocol.mjs
      to: contracts/cohort/m0b-usability-protocol.json
      via: canonical digest and completeness validation
    - from: scripts/cohort/verifyM0BUsabilityProtocol.mjs
      to: src/process/services/cohort/types.ts
      via: mechanical equality with every shared closed M0B runtime enum and fixed protocol constant
    - from: scripts/cohort/verifyM0BUsabilityProtocol.mjs
      to: src/common/types/cohortRollout.ts
      via: mechanical equality with shared return-reason vocabulary
---

<objective>
Freeze a complete machine-validated M0B usability protocol before observation authorization.

Terminal claim: one canonical digest binds every script, segment, rubric, threshold, denominator, minimum, stop rule, Classic comparison, decision owner, and shared closed runtime vocabulary before Day 0.
</objective>

<execution_context>
Follow D-02, D-05, D-06, D-12, and D-13. Protocol changes after Day 0 invalidate the observation.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Author the immutable protocol and verifier together</name>
  <files>contracts/cohort/m0b-usability-protocol.json, scripts/cohort/verifyM0BUsabilityProtocol.mjs</files>
  <action>Define closed participant segments using the canonical runtime values established by 01-01; exact novice quick/substantial and five primary-journey scripts; expert interaction budget; disclosure and authority-comprehension rubric; Classic comparison; thresholds and denominators; participants and per-journey minimums; 14-calendar-day soak; all zero-tolerance stops; privacy exclusions; decision owner; signing schema; and invalidation rules. Implement the verifier in the same task and require exact set-and-order equality with `M0B_COHORTS`, `M0B_SHELLS`, `M0B_PRIMARY_JOURNEYS`, `M0B_RETURN_REASONS`, `M0B_SUPPORT_CATEGORIES`, `M0B_ACCESSIBILITY_SEVERITIES`, and `M0B_ZERO_TOLERANCE_REASONS`, plus exact equality with `M0B_SCHEMA_VERSION`, `M0B_OBSERVATION_WINDOW_DAYS`, and `M0B_DAY_MS = 86_400_000` from `src/process/services/cohort/types.ts`; also bind the shared `COCKPIT_RETURN_REASONS` source in `src/common/types/cohortRollout.ts`. No post-start interpretation changes are allowed.</action>
  <verify>node scripts/cohort/verifyM0BUsabilityProtocol.mjs contracts/cohort/m0b-usability-protocol.json</verify>
  <done>The artifact is complete, canonical, closed-category, and deterministically digestible.</done>
</task>
<task type="auto">
  <name>Task 2: Prove completeness and digest stability</name>
  <files>scripts/cohort/verifyM0BUsabilityProtocol.mjs, tests/unit/scripts/cohort/m0bUsabilityProtocol.test.ts</files>
  <action>Complete validation of exact keys, closed enumerations, canonical runtime-vocabulary drift, numeric bounds, denominator coverage, stop rules, owner, privacy, and canonical serialization. Add a hostile drift fixture for each bound runtime enum and fixed constant, covering missing, extra, reordered, case-aliased, and copied-stale values; `M0B_DAY_MS` specifically must reject omission and every value other than integer `86_400_000`. Also reject omitted scripts, segments, denominators, thresholds, owner, Classic comparison, soak, and post-start mutation.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/scripts/cohort/m0bUsabilityProtocol.test.ts && node scripts/cohort/verifyM0BUsabilityProtocol.mjs contracts/cohort/m0b-usability-protocol.json</verify>
  <done>Every missing or altered protocol element fails and identical protocol bytes yield one stable digest.</done>
</task>
</tasks>

<threat_model>
STRIDE: signer/owner identity blocks spoofing; canonical digest detects tampering; frozen protocol prevents repudiation; scripts contain no participant content; invalid protocol fails safely; thresholds cannot be changed to elevate rollout authority.
</threat_model>

<verification>
Run focused test, `bun run lint -- scripts/cohort/verifyM0BUsabilityProtocol.mjs tests/unit/scripts/cohort/m0bUsabilityProtocol.test.ts`, and exact full gate `bun run test` before summary or commit.
</verification>

<success_criteria>
Day 0 can consume one immutable complete protocol digest whose runtime enums and fixed constants match the executable contract exactly, with no copied subset or mutable prose substitute.
</success_criteria>

<output>
Create `01-23-SUMMARY.md` only after `bun run test`; record the canonical digest as implementation evidence, not an observation receipt.
</output>
