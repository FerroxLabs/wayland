---
phase: WLD-01-safety-foundation
plan: 08
type: execute
wave: 2
depends_on: ['01-40']
files_modified:
  - src/common/types/managedWorkspaceRetention.ts
  - src/process/services/workspaceRetention.ts
  - src/process/services/managedWorkspaceInventory.ts
  - src/process/services/managedWorkspaceProvenance.ts
  - src/process/services/desktopManagedWorkspaceInventory.ts
  - src/process/utils/initAgent.ts
  - src/process/task/IWorkerTaskManager.ts
  - src/process/task/WorkerTaskManager.ts
  - src/process/bridge/conversationBridge.ts
  - src/process/bridge/workspaceRetentionBridge.ts
  - tests/unit/workspaceRetention.test.ts
  - tests/unit/managedWorkspaceInventory.test.ts
  - tests/unit/managedWorkspaceProvenance.test.ts
  - tests/unit/desktopManagedWorkspaceInventory.test.ts
  - tests/unit/WorkerTaskManager.workspaceRetention.test.ts
  - tests/unit/initAgentPresetFallback.test.ts
  - tests/unit/conversationBridge.workspaceRetention.test.ts
  - tests/unit/process/bridge/workspaceRetentionBridge.test.ts
  - tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts
  - src/renderer/pages/settings/StorageSettings/ManagedWorkspacesCard.tsx
  - tests/unit/renderer/pages/settings/storage/ManagedWorkspacesCard.dom.test.tsx
autonomous: true
requirements: [SAF-04]
must_haves:
  truths:
    - 'Chat deletion preserves every referenced, scheduled, artifact-bearing, modified, or unknown workspace.'
    - 'Without a portable immutable filesystem snapshot, even empty abandoned shells remain preserved and unknown.'
    - 'Phase 1 has no quarantine, prune, or delete authority.'
  artifacts:
    - path: src/common/types/managedWorkspaceRetention.ts
      provides: shared closed IPC report schema consumed by main and renderer
    - path: src/process/services/workspaceRetention.ts
      provides: preservation-first classification
    - path: src/process/services/managedWorkspaceInventory.ts
      provides: canonical read-only inventory vocabulary and summary projection
    - path: src/process/services/managedWorkspaceProvenance.ts
      provides: installation-bound process-owned creation ledger and exact provenance checks
    - path: src/process/task/WorkerTaskManager.ts
      provides: running and terminating process leases retained until shutdown settles
    - path: src/process/bridge/workspaceRetentionBridge.ts
      provides: read-only review projection
    - path: src/renderer/pages/settings/StorageSettings/ManagedWorkspacesCard.tsx
      provides: non-destructive review-candidate presentation
  key_links:
    - from: src/process/bridge/workspaceRetentionBridge.ts
      to: src/process/services/workspaceRetention.ts
      via: main-owned classifier with no mutation IPC
---

<objective>
Make managed workspace retention preservation-first and classification-only.

Terminal claim: the real `conversation.remove` path cannot remove or quarantine managed workspace bytes, and empty-looking shells remain preserved until a future immutable snapshot authority can prove them safely.
</objective>

<execution_context>
D-07 is the authority ceiling. Do not implement the deferred managed-workspace lifecycle.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Make classification exhaustive and conservative</name>
  <files>src/common/types/managedWorkspaceRetention.ts, src/process/services/workspaceRetention.ts, src/process/services/managedWorkspaceInventory.ts, src/process/services/managedWorkspaceProvenance.ts, src/process/services/desktopManagedWorkspaceInventory.ts, src/process/utils/initAgent.ts, src/process/task/IWorkerTaskManager.ts, src/process/task/WorkerTaskManager.ts, src/process/bridge/conversationBridge.ts, tests/unit/workspaceRetention.test.ts, tests/unit/managedWorkspaceInventory.test.ts, tests/unit/managedWorkspaceProvenance.test.ts, tests/unit/desktopManagedWorkspaceInventory.test.ts, tests/unit/WorkerTaskManager.workspaceRetention.test.ts, tests/unit/initAgentPresetFallback.test.ts, tests/unit/conversationBridge.workspaceRetention.test.ts</files>
  <action>Classify referenced, scheduled, artifact-bearing, modified, active, empty-abandoned, and unknown workspaces from process-owned inventory evidence. Default missing, malformed, raced, unreadable, incomplete, or contradictory evidence to preserve/unknown. Establish provenance only through an installation-bound process-owned creation ledger written at the actual temporary-workspace creation seam; filename grammar is discovery only and never provenance. Bind provenance to the canonical root, canonical candidate, and stable object identity. Because portable Node filesystem APIs cannot take an immutable directory snapshot, keep review-candidate classification locked until a future snapshot authority exists; path and content races remain preserve/unknown. Keep running and terminating worker leases authoritative until shutdown has actually completed, and make the real `conversation.remove` path await successful shutdown before deleting the persisted conversation reference. Prove byte preservation through the real conversation service/repository and task lifecycle seams. Canonically sort authority projections and reject contradictory duplicates.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/workspaceRetention.test.ts tests/unit/managedWorkspaceInventory.test.ts tests/unit/managedWorkspaceProvenance.test.ts tests/unit/desktopManagedWorkspaceInventory.test.ts tests/unit/WorkerTaskManager.workspaceRetention.test.ts tests/unit/conversationBridge.workspaceRetention.test.ts</verify>
  <done>Hostile and race cases preserve content; the production deletion path preserves workspace bytes; no current filesystem observation can mint review-candidate authority.</done>
</task>
<task type="auto">
  <name>Task 2: Restrict the bridge to read-only review</name>
  <files>src/common/types/managedWorkspaceRetention.ts, src/process/bridge/workspaceRetentionBridge.ts, src/renderer/pages/settings/StorageSettings/ManagedWorkspacesCard.tsx, tests/unit/process/bridge/workspaceRetentionBridge.test.ts, tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts, tests/unit/renderer/pages/settings/storage/ManagedWorkspacesCard.dom.test.tsx</files>
  <action>Expose classification and explanation only through one shared closed IPC schema in `src/common`. Validate the main response before renderer state admission. Project only `reviewCandidate` in the renderer summary and label it as later human review, never quarantine or cleanup eligibility. Show an explicit human-readable active-work reason rather than leaking the internal `active` token. Reject path injection, arbitrary roots, renderer-supplied classifications, mutation verbs, legacy `quarantineEligible` aliases, malformed report shapes, and unknown IPC fields. Ensure no quarantine/delete/prune capability is registered or returned.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/process/bridge/workspaceRetentionBridge.test.ts tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts tests/unit/renderer/pages/settings/storage/ManagedWorkspacesCard.dom.test.tsx && bun run typecheck</verify>
  <done>Renderer can inspect conservative classifications but cannot mutate workspace content or authority.</done>
</task>
</tasks>

<threat_model>
STRIDE: renderer classifications and paths cannot spoof authority; filesystem races fail preserve; decisions are explainable; paths/content are not over-disclosed; unreadable state is safe; no IPC grants destructive privilege.
</threat_model>

<verification>
Run focused tests, `bun run lint -- src/process/services/workspaceRetention.ts src/process/services/managedWorkspaceInventory.ts src/process/services/desktopManagedWorkspaceInventory.ts src/process/bridge/conversationBridge.ts src/process/bridge/workspaceRetentionBridge.ts src/renderer/pages/settings/StorageSettings/ManagedWorkspacesCard.tsx tests/unit/workspaceRetention.test.ts tests/unit/managedWorkspaceInventory.test.ts tests/unit/desktopManagedWorkspaceInventory.test.ts tests/unit/conversationBridge.workspaceRetention.test.ts tests/unit/process/bridge/workspaceRetentionBridge.test.ts tests/unit/bridgeAllowlistWorkspaceRetention.redteam.test.ts tests/unit/renderer/pages/settings/storage/ManagedWorkspacesCard.dom.test.tsx`, typecheck, and exact full `bun run test` before summary or commit; require `rg -n "quarantine-eligible|quarantineEligible"` over every owned source/test path to return no matches and inspect the bridge/UI for all delete/quarantine/prune verbs.
</verification>

<success_criteria>
All current work is preserved; the read-only UI can explain future non-destructive review candidates but cannot mint or act on them.
</success_criteria>

<output>
Create `01-08-SUMMARY.md` only after the full gate and explicitly state that lifecycle mutation is not delivered.
</output>
