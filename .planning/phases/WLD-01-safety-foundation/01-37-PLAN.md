---
phase: WLD-01-safety-foundation
plan: 37
type: execute
wave: 2
depends_on: ['01-40']
files_modified:
  - .planning/execution/PACKET-GATES.json
  - .planning/execution/PACKET-CONTRACTS.json
  - .planning/execution/packet-gate-lib.mjs
  - .planning/execution/wayland-gsd-gate.mjs
  - .planning/execution/check-packet-gate.test.mjs
  - .planning/execution/packet-gate-manifest.test.mjs
  - .planning/execution/cross-worktree-receipt.test.mjs
  - .planning/execution/README.md
  - .planning/execution/TARGET-VERIFICATION-AMENDMENT.md
  - .planning/phases/WLD-01-safety-foundation/01-37-SUMMARY.md
autonomous: true
requirements: [SAF-05]
must_haves:
  truths:
    - 'Acceptance gates authenticate declared targets separately from prerequisites.'
    - 'Every current P1-P7 gate is explicitly classified as entry or acceptance; mixed or unclassified schema fails closed.'
    - 'An acceptance gate with no target is invalid and fails closed.'
    - 'Repository implementation is not installed external authority.'
  artifacts:
    - path: .planning/execution/PACKET-GATES.json
      provides: schema-v2 prerequisite and accepted-target declarations
    - path: .planning/execution/packet-gate-lib.mjs
      provides: independent prerequisite and target verification
    - path: .planning/execution/check-packet-gate.test.mjs
      provides: hostile target-authority regression proof
  key_links:
    - from: .planning/execution/PACKET-GATES.json
      to: .planning/execution/packet-gate-lib.mjs
      via: prerequisites and accepts schema-v2 fields
    - from: .planning/execution/packet-gate-lib.mjs
      to: verifier result
      via: separate prerequisites and accepted_targets output
---

<objective>
Implement the bounded repository-side schema-v2 target-verification source and hostile proof.

Terminal claim: repository gate source rejects target-free acceptance, authenticates declared targets separately from prerequisites, and reports both results without claiming external installation.
</objective>

<execution_context>
Implement only the source contract in `.planning/execution/TARGET-VERIFICATION-AMENDMENT.md`. This plan does not edit `/Users/seandonahoe/.local/**` or `/Users/seandonahoe/.config/**`, provision a key, authorize a packet, or claim the installed verifier changed. Plan 01-38 owns installation and pin proof.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Separate prerequisite and accepted-target schema</name>
  <files>.planning/execution/PACKET-GATES.json, .planning/execution/PACKET-CONTRACTS.json, .planning/execution/packet-gate-manifest.test.mjs</files>
  <action>Upgrade the complete sealed manifest atomically to schema version 2. Classify every existing P1-P7 gate from the amendment as `mode: entry` or `mode: acceptance`; entry gates keep exact prerequisite/exclusive semantics and omit targets, while acceptance gates declare non-empty `accepts`. Add bounded `M0B-OBSERVATION-COMPLETE`, dedicated `P1-M0B` and `P1-FLUX-PRODUCER` acceptance gates, and `M8` as the target of `P5-M8-ACCEPTANCE`. Preserve every existing P2-P7 dependency and physical-absence branch. Reject unclassified gates, mixed v1/v2 fields, unknown contracts, entry targets, empty acceptance targets, duplicate target/prerequisite ambiguity, and v1 authorization replay.</action>
  <verify>node --test .planning/execution/packet-gate-manifest.test.mjs</verify>
  <done>Every current gate is explicitly classified; all acceptance gates declare exact targets, all entry gates are non-promoting, and mixed/target-free acceptance schema is invalid.</done>
</task>
<task type="auto">
  <name>Task 2: Enforce target authority in the repository verifier source</name>
  <files>.planning/execution/packet-gate-lib.mjs, .planning/execution/wayland-gsd-gate.mjs</files>
  <action>Validate schema mode before receipts. For acceptance gates, validate prerequisites and accepted targets independently through the existing signature, contract, candidate, tree, registry, evidence, environment, ancestry, and gate-object digest checks; success requires both. For entry gates, validate prerequisites only, emit `mode: entry`, and never return a green accepted target. Emit distinct `prerequisites` and `accepted_targets`; never infer a target from top-level `ok`. Keep the executable shape `wayland-gsd-gate &lt;gate-id&gt;`. Do not weaken external trust-root, controlled-path, or exact-HEAD checks.</action>
  <verify>node --test .planning/execution/check-packet-gate.test.mjs .planning/execution/cross-worktree-receipt.test.mjs</verify>
  <done>Missing/wrong/stale targets fail independently even when every prerequisite is green.</done>
</task>
<task type="auto">
  <name>Task 3: Seal hostile proof and bounded documentation</name>
  <files>.planning/execution/check-packet-gate.test.mjs, .planning/execution/packet-gate-manifest.test.mjs, .planning/execution/cross-worktree-receipt.test.mjs, .planning/execution/README.md, .planning/execution/TARGET-VERIFICATION-AMENDMENT.md</files>
  <action>Add hostile cases for unclassified gate, mixed v1/v2 object, entry gate with target, acceptance gate with empty/missing target, citing an entry result as acceptance, prerequisite-only green, wrong target, stale v1 authorization, both/neither exclusive targets, sibling commit/tree substitution, and green prerequisites with red target. Enumerate all current P1-P7 gates in manifest tests so no gate is silently omitted. Document that repository source is non-authoritative until 01-38 externally installs and pins the exact committed bytes.</action>
  <verify>node --test .planning/execution/*.test.mjs &amp;&amp; bun run lint -- .planning/execution &amp;&amp; bun run test</verify>
  <done>All focused hostile tests, targeted lint, and the exact full suite pass before summary or commit.</done>
</task>
</tasks>

<threat_model>
STRIDE: explicit target identity prevents prerequisite spoofing; complete gate digests prevent schema substitution; signed exact-candidate receipts prevent repudiation; stable coded and sanitized results avoid disclosure; missing targets fail closed; vacuous prerequisite success cannot elevate acceptance.
</threat_model>

<verification>
Run the focused manifest/verifier hostile suites, `bun run lint -- .planning/execution`, and exact full `bun run test` before summary or commit. Do not claim the installed external verifier changed.
</verification>

<success_criteria>
Schema-v2 repository source and hostile proof are green, while external authority remains explicitly pending 01-38.
</success_criteria>

<output>
Create `01-37-SUMMARY.md` only after full local proof, with exact source commit/tree and digests plus the explicit external-installation non-claim.
</output>
