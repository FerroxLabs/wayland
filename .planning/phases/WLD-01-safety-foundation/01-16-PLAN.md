---
phase: WLD-01-safety-foundation
plan: 16
type: execute
wave: 3
depends_on: ["01-06"]
files_modified:
  - src/process/services/recovery/recoveryStateTransformer.ts
  - src/process/services/recovery/startupCompatibility.ts
  - tests/unit/process/services/recovery/recoveryStateTransformer.bun.test.ts
  - src/process/services/recovery/startupCompatibility.bun.test.ts
autonomous: true
requirements: [SAF-01, SAF-03]
must_haves:
  truths:
    - "Only ask/trusted-edits persist as canonical authority."
    - "Signed Classic interprets future or unknown state conservatively."
    - "Re-upgrade reapplies source-bound deltas without widening authority."
  artifacts:
    - path: src/process/services/recovery/recoveryStateTransformer.ts
      provides: canonical downgrade/re-upgrade transform
  key_links:
    - from: src/process/services/recovery/startupCompatibility.ts
      to: src/process/services/recovery/recoveryStateTransformer.ts
      via: schema and authority compatibility gate
---

<objective>
Prove canonical authority migration and conservative downgrade/re-upgrade behavior.

Terminal claim: every legacy or future authority state maps to equal-or-less authority in Classic and returns without silent widening on re-upgrade.
</objective>

<execution_context>
Follow D-01, D-07, and SAF-03 exactly; no alternate persistent vocabulary may remain.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Canonicalize authority without widening</name>
  <files>src/process/services/recovery/recoveryStateTransformer.ts, tests/unit/process/services/recovery/recoveryStateTransformer.bun.test.ts</files>
  <action>Migrate stored `chat` to `ask` and `cowork` to `trusted-edits`, reject any other persistent authority vocabulary, and define conservative projections for malformed/unknown/future values. Record source-bound deltas required for re-upgrade and never infer broader authority from model, mode, or feature labels.</action>
  <verify>GSD_RUNTIME=codex bun test tests/unit/process/services/recovery/recoveryStateTransformer.bun.test.ts</verify>
  <done>Exhaustive vocabulary and malformed-state tests prove monotonic non-widening transforms.</done>
</task>
<task type="auto">
  <name>Task 2: Gate startup and delta-safe re-upgrade</name>
  <files>src/process/services/recovery/startupCompatibility.ts, src/process/services/recovery/startupCompatibility.bun.test.ts</files>
  <action>Prevent signed v0.11.8 from opening unclassified schema-53 state directly. Require an isolated transformed tree, exact schema compatibility, and source-bound re-upgrade delta. Reject missing deltas, stale source epoch, unknown schema, direct binary path, and any effective-authority increase.</action>
  <verify>GSD_RUNTIME=codex bun test src/process/services/recovery/startupCompatibility.bun.test.ts && bun run typecheck</verify>
  <done>Old binary startup is isolated and re-upgrade preserves effective authority exactly or fails closed.</done>
</task>
</tasks>

<threat_model>
STRIDE: schema/source identity prevents spoofing; signed deltas detect tampering; transition receipts prevent repudiation; no secret disclosure; incompatible startup fails safely; legacy labels cannot elevate authority.
</threat_model>

<verification>
Run focused Bun tests, `bun run lint -- src tests`, and exact full `bun run test` before summary or commit, then consume only from the signed target matrix.
</verification>

<success_criteria>
SAF-03 is independently proven before any historical binary execution evidence is accepted.
</success_criteria>

<output>
Create `01-16-SUMMARY.md` only after the full gate, with authority mapping table and hostile proof counts.
</output>
