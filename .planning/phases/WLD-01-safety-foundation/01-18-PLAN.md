---
phase: WLD-01-safety-foundation
plan: 18
type: execute
wave: 8
depends_on: ["01-06", "01-07", "01-08", "01-13", "01-19", "01-20", "01-21", "01-22", "01-24"]
files_modified:
  - contracts/wayland-desktop-core/quiescence/v1
  - scripts/supply-chain/importCoreQuiescenceContract.mjs
  - scripts/supply-chain/verifyCoreQuiescenceContract.mjs
  - src/process/services/recovery/coreQuiescenceLease.ts
  - tests/unit/scripts/coreQuiescenceContract.test.ts
  - tests/unit/process/services/recovery/coreQuiescenceLease.test.ts
autonomous: false
requirements: [SAF-01, SAF-03, SAF-04, SAF-05]
must_haves:
  truths:
    - "The harness declares all six target cells and the complete recovery journey."
    - "Core-bearing cells cannot be requested without producer quiescence issue #896 identity."
    - "Construction remains blocked until the published #896 contract, corpus, schema/fixture/source-input digests, and Desktop admission receipt are exact and available."
    - "Implementation, fixture, package, deployment, canary, release, and recovery acceptance remain separate."
  artifacts:
    - path: contracts/wayland-desktop-core/quiescence/v1/manifest.json
      provides: byte-exact imported #896 producer identity and fixture inventory
    - path: scripts/supply-chain/importCoreQuiescenceContract.mjs
      provides: exact-commit Git-object acquisition boundary driven only by the published receipt
    - path: scripts/supply-chain/verifyCoreQuiescenceContract.mjs
      provides: closed manifest, digest, inventory, and receipt verifier
    - path: src/process/services/recovery/coreQuiescenceLease.ts
      provides: producer-admitted Core lease and fixture-replay adapter for the safe 01-06 builder
  key_links:
    - from: contracts/wayland-desktop-core/quiescence/v1/manifest.json
      to: src/process/services/recovery/coreQuiescenceLease.ts
      via: verified producer fixtures replay through the real matrix builder before any Core-bearing cell is requestable
    - from: src/process/services/recovery/coreQuiescenceLease.ts
      to: plan 01-39
      via: immutable complete target-matrix request; never acceptance
---

<objective>
Build the immutable complete six-target M0A harness after local recovery and trust plans pass.

Terminal claim: the local harness can request only a complete six-target install/preserve/inspect/restore/rollback/boot/re-upgrade matrix and cannot mint M0A.
</objective>

<execution_context>
D-02, D-07, D-12, and D-13 apply. Core quiescence #896 identity is a mandatory construction predecessor for every Core-bearing cell. At planning time issue #896 has no published producer contract, manifest, corpus path, or fixture digest, so this plan must not guess them. It is deliberately non-autonomous until a signed Desktop admission receipt declares the published producer repository and ref, full commit and tree, contract name/version, manifest path, corpus path, fixture count, schema digest, fixture digest, source-input digest, and the external admission-verifier identity/digest that authenticated the signature. Keep every existing `depends_on` edge unchanged; 01-39 remains blocked on this plan. Local source presence, issue text, harness proof, or package presence cannot substitute for that admission or for external matrix execution and M0A acceptance.
</execution_context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Admit the exact published #896 producer contract</name>
  <action>Before any source edit, independently authenticate a signed Desktop admission receipt for the published #896 contract. The receipt must bind repository `https://github.com/FerroxLabs/wayland-core.git`, published ref, full producer commit and tree, contract name/version, manifest path, corpus path, fixture count, schema digest, fixture digest, source-input digest, signer, signature, and the exact external admission-verifier version/digest used to authenticate it. Require the referenced commit to be the commit resolved by that published producer ref and the corpus plus hostile fixtures to be present in that exact Git object. Missing, unpublished, locally fabricated, stale, ambiguously addressed, or mismatched evidence keeps this plan non-autonomous and blocked; issue or PR status alone is not admission.</action>
  <verify>Human checkpoint: compare the signed admission receipt against the published Core ref and exact commit/tree, then export `WAYLAND_CORE_REPO` and `CORE_896_RECEIPT` as paths to that authenticated local object database and receipt. There is intentionally no repository-local command that can waive this checkpoint.</verify>
  <done>The exact #896 producer contract and immutable fixture identities are admitted for Desktop construction, without claiming M0A or target acceptance.</done>
</task>
<task type="auto">
  <name>Task 2: Acquire and verify the admitted producer corpus</name>
  <files>contracts/wayland-desktop-core/quiescence/v1, scripts/supply-chain/importCoreQuiescenceContract.mjs, scripts/supply-chain/verifyCoreQuiescenceContract.mjs, tests/unit/scripts/coreQuiescenceContract.test.ts</files>
  <action>Implement an importer that reads every producer identity and the source manifest/corpus path only from the admitted receipt, confirms the receipt was authenticated by the pinned external admission-verifier identity/digest, resolves the published ref plus exact commit and tree in `WAYLAND_CORE_REPO`, and exports that Git object into a temporary directory before atomically installing the byte-exact corpus at `contracts/wayland-desktop-core/quiescence/v1`. Implement an independent verifier that recomputes the complete regular-file inventory plus per-file and aggregate schema/fixture/source-input digests and compares them to both the imported manifest and admission receipt. Reject the wrong origin, verifier drift, unauthenticated receipt, abbreviated or unreachable commit, ref/commit disagreement, tree mismatch, missing/extra/symlink/non-regular files, path escape, duplicate path, count drift, contract/version drift, digest drift, or an existing output with different bytes. Never read the dirty producer worktree and never synthesize absent fixtures.</action>
  <verify>node scripts/supply-chain/importCoreQuiescenceContract.mjs --core-repo "$WAYLAND_CORE_REPO" --receipt "$CORE_896_RECEIPT" --out contracts/wayland-desktop-core/quiescence/v1 && node scripts/supply-chain/verifyCoreQuiescenceContract.mjs --contract-root contracts/wayland-desktop-core/quiescence/v1 --receipt "$CORE_896_RECEIPT" && GSD_RUNTIME=codex bunx vitest run tests/unit/scripts/coreQuiescenceContract.test.ts</verify>
  <done>The committed Desktop corpus is a reproducible byte-exact export of the admitted published Git object, and every identity or inventory near miss fails closed.</done>
</task>
<task type="auto">
  <name>Task 3: Bind the harness to immutable target inputs</name>
  <files>src/process/services/recovery/coreQuiescenceLease.ts, tests/unit/process/services/recovery/coreQuiescenceLease.test.ts</files>
  <action>Build the producer adapter on top of the generic destination-admission and publication-safe builder delivered by 01-06. Build matrix entries from the deterministic Classic journey baseline, exact historical transaction corpus/provenance, packaged-Core authority, Core plus two non-Core authority-adapter ledger, workspace-preservation classification, current-host OfficeCLI authority, source, package, historical release, publisher, environment, Constitution, authority-transform, and the admitted #896 commit/contract/corpus/digests. For Core-bearing cells, consume the producer-owned #896 lease contract and replay its exact fixtures to prove acquisition, exact authority-root scope, expiry, mutation-after-lease rejection, and release without inventing Desktop authority. Keep every evidence state separate and reject missing targets, incomplete corpus inventory, contract or digest drift, duplicate/conflicting cells, wrong architecture, stale environment, partial journey, unsigned artifact, workspace loss, state digest mismatch, missing/expired lease, wrong scope, or post-lease mutation. Do not re-own or weaken generic destination safety. No new locale or generated i18n files are owned.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/process/services/recovery/coreQuiescenceLease.test.ts && bun run typecheck</verify>
  <done>The harness can only request complete immutable cells and cannot aggregate partial or mismatched evidence.</done>
</task>
</tasks>

<threat_model>
STRIDE: target/package/signer identity blocks spoofing; cell and state digests detect tampering; signed journeys prevent repudiation; user content stays in disposable fixtures; missing targets fail safely; no partial or earlier evidence state elevates M0A.
</threat_model>

<verification>
Do not run construction verification until Task 1 admission is green. Then run the exact Task 2 import/verify/test command, `bun run lint -- scripts/supply-chain/importCoreQuiescenceContract.mjs scripts/supply-chain/verifyCoreQuiescenceContract.mjs src/process/services/recovery/coreQuiescenceLease.ts tests/unit/scripts/coreQuiescenceContract.test.ts tests/unit/process/services/recovery/coreQuiescenceLease.test.ts`, the Task 3 exact producer-lease replay command, typecheck, and exact full `bun run test` before summary or commit. No external execution, acceptance, merge, release, deployment, or real-profile mutation.
</verification>

<success_criteria>
The harness rejects incomplete/mismatched matrices and emits no acceptance state; plan 01-39 exclusively owns external execution and M0A acceptance.
</success_criteria>

<output>
Create `01-18-SUMMARY.md` only after the full local gate, with exact harness proof and explicit M0A/external-execution non-claims.
</output>
