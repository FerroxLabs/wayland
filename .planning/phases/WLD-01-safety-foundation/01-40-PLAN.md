---
phase: WLD-01-safety-foundation
plan: 40
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/execution/desktop-gsd-next.mjs
  - .planning/execution/desktop-gsd-next.test.mjs
  - .planning/execution/DESKTOP-GSD-ADMISSION.json
  - .planning/execution/DESKTOP-GSD-OPERATOR.md
autonomous: true
requirements: [SAF-05]
must_haves:
  truths:
    - "Desktop execution selects only dependency-unlocked autonomous construction plans and never equates selection with acceptance."
    - "Every selected builder is bound to a manually created clean isolated worktree before execution."
    - "Phase 1 remains open while authenticated entry gates may unlock explicitly mapped Phase 2-4 construction; Phase 5-6 remain closed."
  artifacts:
    - path: .planning/execution/desktop-gsd-next.mjs
      provides: read-only deterministic plan selector and worktree preflight
    - path: .planning/execution/desktop-gsd-next.test.mjs
      provides: hostile orchestration regression proof
    - path: .planning/execution/DESKTOP-GSD-ADMISSION.json
      provides: default-deny phase/plan to schema-v2 entry-gate admission map
    - path: .planning/execution/DESKTOP-GSD-OPERATOR.md
      provides: source-grounded operator contract for manual worktrees, serial integration, and open sentinels
  key_links:
    - from: .planning/phases/*/*-PLAN.md
      to: .planning/execution/desktop-gsd-next.mjs
      via: parsed dependencies, autonomy, construction summaries, sealed admissions, file ownership, and effective-wave guard
    - from: .planning/execution/PACKET-GATES.json
      to: .planning/execution/DESKTOP-GSD-OPERATOR.md
      via: authenticated entry-gate admission without phase-completion authority
---

<objective>
Install the bounded execution adapter required to run this intentionally open Phase 1 safely and quickly under GSD.

Terminal claim: a read-only selector identifies only the next dependency-safe autonomous construction set, proves its worktree prerequisites, and emits operator instructions without changing source, plan status, receipts, acceptance, or Git state.
</objective>

<execution_context>
Stock `gsd-progress --next` and unscoped/full `gsd-execute-phase` are prohibited for this milestone because they resume the lowest incomplete phase and attempt all incomplete waves, including long-lived human acceptance sentinels. This adapter is a project-specific GSD execution shim, not a replacement evidence authority. It never creates a worktree, launches an agent, integrates a commit, writes a summary, changes a requirement checkbox, invokes an acceptance gate, or marks Phase 1 complete.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build the deterministic read-only selector</name>
  <files>.planning/execution/desktop-gsd-next.mjs, .planning/execution/DESKTOP-GSD-ADMISSION.json</files>
  <action>Parse all existing `.planning/phases/*/*-PLAN.md` frontmatter and SUMMARY presence directly from disk. Compute dependency-derived effective waves over the autonomous construction lattice rather than trusting handwritten wave prose: an unrelated incomplete human checkpoint never blocks an unlocked construction plan, while a checkpoint explicitly named as a dependency remains blocking. Select only plans with `autonomous: true`, all construction predecessor summaries present, no incomplete lower construction wave on the same dependency lattice, and every explicitly mapped external admission authenticated. Keep a sealed, default-deny phase/plan-to-entry-gate map: Phase 1 local construction needs no invented target; mapped Phase 2-4 construction invokes the installed schema-v2 verifier for the exact mapped entry gate and requires `mode: entry`, green prerequisites, and `accepted_targets: []`. Reject acceptance-mode gate IDs, any non-empty accepted target, absent verifier, v1 output, gate mismatch, or forged checkpoint SUMMARY. Phase 5 and Phase 6 are hard-denied. Enforce a bounded parallel set whose files, directory ownership, shared state, schemas, generated outputs, lockfiles, configuration, migration sequence, and named authority seams are disjoint; otherwise return a deterministic serialized order or fail closed. Validate expected repository root, branch, clean tree, and exact HEAD supplied by the operator. Output JSON containing candidate plan IDs, effective construction wave, exact plan paths, owned files, authenticated admissions, proposed worktree names, and next commands. Perform no writes and spawn nothing.</action>
  <verify>node --test .planning/execution/desktop-gsd-next.test.mjs</verify>
  <done>The same exact planning tree yields one deterministic dependency-safe construction set, while blocked or ambiguous state exits nonzero without mutation.</done>
</task>
<task type="auto">
  <name>Task 2: Define the open-phase operator contract</name>
  <files>.planning/execution/DESKTOP-GSD-OPERATOR.md</files>
  <action>Document the exact operating loop: run the selector read-only; manually create one clean isolated worktree from the declared integration HEAD for each selected plan; bind one generic GSD/Codex builder to that exact worktree and PLAN; re-run focused proof; integrate one commit at a time; rebase/re-prove the next overlapping seam; and create a SUMMARY only after the plan terminal claim is green. Prohibit stock `--next`, unscoped/full phase execution, direct work in the planning/integration worktree, concurrent integration, and acceptance inferred from code or SUMMARY presence. After the sealed admission map and installed schema-v2 verifier authenticate exact entry gates, allow only mapped dependency-safe Phase 2-4 construction while the Phase-1 M0B/aggregate sentinels and requirement boxes remain open. Keep all unmapped work default-denied and Phase 5/6 hard-denied. Sean-only merge-to-main, issue close, release, deploy, and cohort promotion remain untouched.</action>
  <verify>node --test .planning/execution/desktop-gsd-next.test.mjs -t "operator contract"</verify>
  <done>An operator can resume after context loss without stock-GSD misrouting or confusing construction progress with acceptance.</done>
</task>
<task type="auto">
  <name>Task 3: Attack selection and worktree authority</name>
  <files>.planning/execution/desktop-gsd-next.test.mjs, .planning/execution/desktop-gsd-next.mjs, .planning/execution/DESKTOP-GSD-ADMISSION.json, .planning/execution/DESKTOP-GSD-OPERATOR.md</files>
  <action>Add executable hostile fixtures for a non-autonomous/checkpoint plan, missing construction-predecessor SUMMARY, an unrelated incomplete lower-wave checkpoint that must not block, an explicitly depended-on checkpoint that must block, forged checkpoint SUMMARY without authenticated target receipt, incomplete lower autonomous construction wave, cyclic/unknown dependency, same-wave exact-file overlap, directory-child overlap, shared lock/schema/config/generated/migration seam, dirty tree, wrong repository root, wrong branch, stale HEAD, malformed frontmatter, unmapped Phase 2-4 admission, absent/v1/wrong-gate verifier output, and attempts to select Phase 5/6 or treat an entry gate as acceptance. Prove a clean disjoint set is stable and that the program produces byte-identical source/Git status before and after every pass/fail invocation.</action>
  <verify>node --test .planning/execution/desktop-gsd-next.test.mjs &amp;&amp; bun run lint -- .planning/execution/desktop-gsd-next.mjs .planning/execution/desktop-gsd-next.test.mjs &amp;&amp; git diff --exit-code -- . ':!.planning/execution/desktop-gsd-next.mjs' ':!.planning/execution/desktop-gsd-next.test.mjs' ':!.planning/execution/DESKTOP-GSD-ADMISSION.json' ':!.planning/execution/DESKTOP-GSD-OPERATOR.md'</verify>
  <done>Blocked, stale, overlapping, dirty, wrong-worktree, and acceptance-confusing selections fail closed; the adapter remains read-only.</done>
</task>
</tasks>

<threat_model>
STRIDE: root/branch/HEAD identity blocks worktree spoofing; plan and ownership parsing exposes tampering; deterministic output supports repudiation checks; no secrets or receipt contents are emitted; cycles and malformed plans fail without denial-by-spin; read-only selection cannot elevate construction into acceptance.
</threat_model>

<verification>
Run the complete adapter hostile suite, targeted lint, a clean-tree before/after comparison, and exact full `bun run test` before summary or commit. This plan proves orchestration selection only; it cannot accept a packet or complete a phase.
</verification>

<success_criteria>
Desktop GSD execution has a tested project-specific next-work selector that respects dependency, ownership, worktree, integration, and evidence boundaries without stock-GSD phase-completion deadlock.
</success_criteria>

<output>
Create `01-40-SUMMARY.md` only after the selector and hostile operator proof are green, with exact commit/tree and no acceptance claim.
</output>
