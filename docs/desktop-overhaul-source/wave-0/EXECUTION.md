# Wave 0 execution control

Native Constitution production authority is additionally governed by
`NATIVE-CONSTITUTION-V2-ACCEPTANCE.md`. The locally green predecessor
`991c502e74506ec3702f92e429a8b31b655412ba` is rejected and quarantined until a
new immutable candidate satisfies that paired v2 gate and independently returns
zero unresolved HIGH/BLOCKER findings.

Baseline: Desktop `v0.11.18` (`1b1c1e91119e3352bec3958188254ee91f150492`)  
Branch: `codex/desktop-cockpit-wave0`  
Desktop issue: `FerroxLabs/wayland#886`  
Core handoff: `FerroxLabs/wayland#887`  
Flux handoff: `FerroxLabs/wayland#888`

## Authorization boundary

Wave 0 may change Desktop recovery, compatibility, contract, fixture, and proof infrastructure. Sean's 2026-07-16 corrective-scope amendment also permits C0 contract/supply-chain/consent fixes and the bounded M1M/MCP-0 exception defined in `MASTER-BUILD-PLAN.md`: truthful saved/auth/probe/publication copy; fail-closed repair of existing add/import/publication transactions; URL safety/identity and existing-adapter token-refresh correction; ACP Team acceptance-event renaming; and non-promoting deterministic publication/session/vendor-shaped fixtures and receipt reducers. Named-canary work before M8 means fixture preparation only, never live credential use or a vendor-parity claim. No persistent MCP lifecycle migration/store, automatic session restart, ToolSearch/readiness promotion, invented backend receipt, packaged/cohort claim, or M2/M5/M7 expansion is authorized before its dependency. Existing Cockpit shell/navigation code is an isolated prototype on copied/disposable state—not accepted M3/M4 product work—and further shared-model expansion remains flagged until M0A passes. Real-user enrollment remains blocked until M0A, M0B, M1/M1F capability gates, MCP truth gates, and release evidence pass.

## Parallel streams

1. M0A engineering safety: higher-schema refusal, state inventory, backup/restore validation, recovery launcher, updater isolation, and six-target receipts.
2. M0B cohort authority: Classic baseline capture and signed threshold table, observing for 14 calendar days from Day 0.
3. M1/M1F contracts: runtime validation, fixture replay, real process/IPC harness, and explicit degraded capabilities.

## Global stops

Data corruption, approval widening, cross-Project leakage, forged verification, or failed recovery stops all cohort promotion. A producer-contract miss disables only the dependent capability when a named degraded profile exists; it never produces inferred evidence.

Until the isolated schema-52 transformer, external recovery launcher, and signed
six-target downgrade/re-upgrade journey pass, `v0.11.18` is the operational
recovery floor. The requested `v0.11.8` target remains an open engineering gate,
not a launcher option against live schema-53 state and not an accepted rollback
claim.

## Receipt index

- `receipts/M0A.json`
- `receipts/M0A-ENGINEERING-SAFETY-CANDIDATE.md` (local partial proof; not a pass receipt)
- `receipts/M0A-WORKSPACE-RETENTION-CANDIDATE.md` (read-only inventory/UI; no cleanup authority)
- `receipts/M0B.json`
- `receipts/M0B-BASELINE-MACHINERY-CANDIDATE.md` (privacy/aggregation partial proof; observation not started)
- `receipts/M1.json`
- `receipts/M1F.json`
- `receipts/WAVE-0.json`
- `receipts/SBX-0-SETTINGS-TRUTH-CANDIDATE.md` (focused truth correction; not M1S closure)
- `receipts/WAVE-0-STRIKE-CHECKPOINT.md` (immutable local source checkpoint; not packet or release acceptance)
- `HOSTED-CONSTITUTION-REMEDIATION.md` (authorized corrective packet; blocked only on accepted hostile-filesystem dependency)
- `HOSTED-CONSTITUTION-ACCEPTANCE.md` (bounded pre-v2 renderer behavior
  prerequisite at `de9f3c9a`; durable v2 renderer lifecycle, native/process
  route-client, recovery, package, and aggregate Wave 0 acceptance remain
  locked)
- `CONSTITUTION-RECOVERY-ACCEPTANCE.md` (metadata-only archive inventory,
  fresh-password CAS restore, authenticated Classic promotion/rescue/discard,
  partial/conflict visibility, receipt-only success, and route/IPC journey
  contract; Stage B implementation may follow a locally sealed Stage A only in
  the isolated non-promoting worktree/test boundary. Stage A and Stage B remain
  unaccepted and may not enter root until their exact commits are composed,
  journey-proved, sealed, and independently accepted by Stage C)

Receipts are generated only from exact commands and artifacts. An absent or skipped proof remains absent; it is never represented as passed.

### Strike Mode integration control — 2026-07-16

- The previous shared dirty tree is sealed locally at commit
  `f6f7a81950c1ee2ca1629bbcc5c5e78045eb2fa2`, tree
  `191ddfd1cbe60cc4934ce1586d9996eb7766ef9e`.
- Three original isolated worktrees started from those exact bytes: Safety owns
  Constitution recovery, Runtime Truth owned MCP active-session evidence, and
  Shell Resilience owned Classic/Cockpit fault isolation. A fourth Packaging
  worktree now starts from integration commit `623dace07` and owns only the
  exact-target/fresh-resource build corrections identified by the later audit.
- File ownership is exclusive. Each lane must produce a local commit and
  focused proof; root integration adversarially reviews and cherry-picks one
  lane at a time. No lane may push, merge, release, deploy, close issues, or
  promote a receipt.
- Checkpoint TypeScript passed. The focused pre-split baseline passes 9 files / 
  60 tests across Constitution archive primitives, MCP recovery/truth, hosted
  MCP routes, and shell/navigation state.
- Root has integrated the full Vitest/Bun-native test authority, bounded MCP
  exact-session truth, Shell composition-root correction, target-aware
  packaging correction, and the bounded hosted Constitution grant/autosave
  foundation. The bounded hosted candidate is
  `61d79d22c538998e0a76371eea343df93f781df3`; after a rejected native
  cherry-pick and explicit revert, the current exact root is
  `12ea88caf3cd6e490a054060ea96b0f60966bfd8`. Both resolve to tracked tree
  `3c1ebc9a643e8efdc205b909bfc0e14b05321f38`. That tree passes TypeScript,
  1,330 Vitest files / 13,734 tests with 139 intentionally skipped, all 26
  Bun-native files / 191 tests, focused lint across the 21 hosted files with
  zero warnings/errors, diff validation, the production Web renderer build,
  and the server bundle build. The root worktree is clean. Known repository
  lint and bundler warnings remain recorded and are not represented as new
  errors in this slice.
- The first Shell delta was rejected because it isolated a sider rather than
  independent composition roots. Follow-up `fe685db9` independently re-audited
  with zero HIGH and is integrated as `623dace07`; focused root proof passes
  1 file / 7 tests, TypeScript, scoped zero-warning lint, i18n structural
  validation, and diff validation. It remains bounded Shell resilience, not M3
  acceptance or proof against arbitrary shared-route/Classic-chunk failure.
- The first Constitution history/restore delta is rejected pending correction
  of five HIGH integrity, symlink, TOCTOU, CAS, and hosted-error-boundary
  findings. A separate audit found three HIGH pre-existing hosted Constitution
  journey gaps: autosave authorization mismatch, disconnected specialist
  management, and read failure collapsing into reset. Commit `61d79d22c`
  closes the autosave-authorization finding as a bounded candidate: scoped
  digest-only grants, destructive-authority separation, serialized/coalesced
  autosave, durable principal-scoped draft recovery, and explicit retry have
  independent zero-HIGH review. Hosted read truth and specialist inventory/
  hydration remain open, and full hosted acceptance remains locked behind the
  accepted anchored filesystem/CAS/archive backend. Neither native nor hosted
  acceptance may be inferred from this bounded receipt.
- **Bounded pre-v2 hosted renderer correction:** exact clean candidate
  `de9f3c9adb4203654667c4feea7f8d44a7d7668e` supersedes the earlier bounded
  renderer state. The first correction `09148604c` was rejected for silent
  conflict adoption, specialist-delete/autosave races, and reset receipt loss;
  `d1374911d` closed those but was rejected for missing synchronous delete
  ownership and truthy success discriminants. `de9f3c9a` closes the remaining
  intrinsic pre-v2 findings. Exact proof is 6 files / 48 focused tests, 2,114 passed
  and 2 skipped across the full 282-file renderer suite, TypeScript, lint with
  zero errors, diff validation, clean worktree, and independent zero-HIGH/
  BLOCKER review inside that bounded scope. It does not satisfy the new durable
  single-shot v2 renderer lifecycle, is not yet in root, and does not accept
  the native/process route-client seam, recovery, package, or aggregate journey.
- **Rejected native production predecessor:** isolated branch
  `codex/desktop-constitution-production` was sealed and clean at
  `991c502e74506ec3702f92e429a8b31b655412ba`, but immutable review rejected it
  and the branch now contains an unsealed remediation worktree. Historical
  candidate-worktree proof passed
  154/154 combined integration/authority tests, 39/39 native filesystem tests,
  strict Clippy, Rust formatting, full TypeScript, 47/47 package tests, and
  diff validation. The exact staged/codesigned Darwin ARM64 helper digest is
  `sha256:141e9ec8e2163a31d4be124dcaa0dbb4cffddf7295b8bd6fd17d9ecc4559bd17`.
  The historical registered-route -> actual renderer-fetch-client proof covered
  response-loss replay-first behavior for main write, reset, specialist write,
  and delete. Root remains clean at `12ea88caf`; the rejected commit is
  quarantined and cannot be integrated. **The first audit pass rejected
  `991c502e7` for integration:** the real service's already-present main/reset
  replay branch recomputes the mutation fingerprint with a synthetic absent
  revision, so same-request response-loss replay conflicts with its own
  committed mutation. The route-client test used a mocked service and did not
  expose this. A real-service present-update/reset replay correction and rerun
  are required before a new exact commit can be integration-eligible. The same
  audit also finds packaged Windows startup unsafe: packaging correctly omits
  the unsupported helper, but main-process module initialization constructs the
  production service unconditionally and throws `CONSTITUTION_FS_UNSAFE_PLATFORM`.
  Windows must boot with an explicit unavailable capability that refuses these
  reads/mutations without direct-filesystem fallback; package-helper absence is
  not a Windows startup test.
  The completed audit verdict is **NO-GO** with seven release-significant
  integration gaps: durable original-fact replay after service restart;
  mandatory producer-bound request UUIDs; crash-persisted uncertainty for
  autosave and single-shot reset/delete/create/overwrite; atomic legacy-source
  migration; authenticated reconciliation before read/list/CAS; real
  service/native-helper -> Express -> actual fetch-client replay proof; and
  Windows capability degradation without app-start failure. Opaque revisions
  also require durable key continuity across reboot. Electron read/list and
  hosted failure envelopes remain partial until runtime-exact validation is
  proven. No `991c502e7` test count overrides this source-traced verdict.
  The active unsealed replacement is governed by
  `NATIVE-CONSTITUTION-V2-ACCEPTANCE.md`; partial mandatory-ID,
  unsupported-capability, prompt-fail-closed, or autosave proof cannot promote
  it before durable revision authority, restart replay, reconciliation,
  migration, single-shot identity, real composition, package proof, and a new
  immutable zero-HIGH audit all pass.
- Follow-up `7f7a20849` passed 57 focused tests and closes fixed error prose,
  legacy list-only restore, and static POSIX symlink cases, but independent
  source tracing still finds three HIGHs: public-hash forgery, parent/path swap
  races, and loss of the recovery reservation/late displaced bytes. It remains
  outside root integration while an authenticated, transaction-bound successor
  is built and re-audited.
- Constitution parent-swap acceptance cannot be satisfied by repeated pathname
  checks: repository sandbox code already records Node's missing `openat()` and
  final-component-only `O_NOFOLLOW` boundary. Use a real directory-anchored
  platform primitive or fail the destructive operation closed on unsupported
  hosts. Carry an authenticated recovery reservation through commit and retain
  or revalidate displaced bytes before destruction.
- Work is split accordingly: the Safety lane is removing JS pathname fallback
  while sealing HMAC v3/legacy/recovery contracts, and an isolated
  `codex/desktop-strike-constitution-fs` lane owns a standalone anchored native
  transaction helper plus hostile swap tests. Neither lane may claim the HIGH
  closed alone; integration, packaging identity, and independent re-audit are
  required.
- The first native-helper work-in-progress is explicitly rejected for
  integration until it stops synthesizing unauthenticated version-2 history,
  implements anchored restore/retirement as well as replace/delete, reconciles
  retained crash journals, rejects non-regular/unsafe-link files, binds every
  receipt field to the exact request, proves real TypeScript-to-native wire
  compatibility, and closes or honestly bounds its verified-binary-to-exec
  pathname race. The fake-executor wrapper suite is not native protocol proof.
  These are sub-findings inside the existing five Constitution HIGHs, so the
  aggregate count does not change merely because the WIP has grown.
- The continuing native attack adds mandatory acceptance checks without
  changing that count: originally verified helper identity must survive a
  coordinated helper+manifest replacement; every filesystem-effect/journal
  gap in replace, delete, restore, archive publication, destination
  displacement, and source retirement needs write-ahead state plus
  fact-derived crash reconciliation; rollback may use only a held trusted
  snapshot; retirement must bind final inode and digest; and live
  Constitution/specialist/archive reads and inventories must be anchored and
  non-creating. Mutation-only coverage does not close prompt-read ancestor
  swaps. Each injected crash must prove reconciliation followed by a new
  successful transaction.
- Sealed native source commit
  `6ddcdefa003e1b29fc15654dc477e42c510703df` passes its bounded ordered proof:
  39 native tests, 22 focused TypeScript tests, isolated TypeScript, strict
  Clippy, Rust formatting, and diff validation after an explicit native build.
  It is zero-HIGH only for its low-level transaction/receipt/replay boundary,
  not for production integration or clean-root CI.
- Root integration attempt `a2debfd66ed4385af0e9e9b258f153469e6f5e85` is rejected and reverted at
  `12ea88caf3cd6e490a054060ea96b0f60966bfd8`. Cold focused proof reproduced `ENOENT` because the real-helper
  test assumed a precompiled `target/debug` binary; full root TypeScript
  reproduced an executor-path type error because the declared descriptor-only
  union omitted the Darwin private snapshot path. The current tracked tree is
  again exactly `3c1ebc9a643e8efdc205b909bfc0e14b05321f38`, matching the
  bounded hosted baseline; the native candidate remains isolated.
- Restart reconciliation remains a HIGH. Pending inventory exposes only IDs,
  but reconcile requires exact authenticated operation/target/expected/
  replacement/archive/restore/recovery facts retained only in process memory.
  Add authenticated pending details or an equivalent authenticated intent
  authority and prove crash -> process restart -> exact reconcile -> next
  transaction, including tamper and changed-fact rejection.
- Production acceptance additionally requires a sole Constitution filesystem
  service/lifetime owner, durable journal/archive key ownership and
  loss/rotation/migration behavior, shared bridge/Web/prompt injection,
  retirement of the disconnected v1 direct-filesystem archive path, a
  non-test independently pinned helper authority, target-exact staging,
  signing/notarization-aware packaged-byte verification, clean-root CI, and
  explicit Windows parity or honest fail-closed capability. Integration order
  is cold-root fixes -> reboot protocol -> service/key owner -> consumer
  cutover -> package/sign/verifier -> CI -> packaged journeys -> independent
  aggregate re-audit.
- The dual-architecture package run independently found four HIGH defects:
  cross-output architecture verification, false-green Signal extraction
  debris, host- rather than package-target Signal selection, and a stale/
  wrong-architecture DMG retry resolver. Commit
  `791fccb3466209ec4b9bd8140194c22716a8f7c7` is now integrated with exact-target
  and resource-verification corrections and focused source proof. Packaging
  acceptance nevertheless remains HOLD until the integrated native helper is
  staged through the same authority, the full six-target/fresh-output package
  matrix and post-pack verifier run, signed/notarized execution evidence is
  captured, and the aggregate package result receives independent zero-HIGH
  review.
- Packaging degradation is absence-only. A present malformed, stale, or
  unauthenticated optional Hub, WhatsApp, or Signal bundle must fail the release
  gate; Hub must remain absent while its immutable authority is unavailable.
  Requested platform and architecture own Bun preparation. The models.dev
  floor, voice model, and Bun executable are exact-byte pinned. WhatsApp release
  packaging must start from a clean frozen `bun.lock` dependency install and
  prove an exact recursive package/input mirror with safe in-tree links; a
  mutable fallback install or dirty `node_modules` comparison is not authority.
- The split accelerates independent remediation only. It does not unlock M2,
  C1+, MCP-2, M0A/M0B acceptance, live credentials, packaging, enrollment, or
  cohorts.

### SBX-0 settings truth — 2026-07-16

- Removed ineffective Security-pane switches whose apparent writes did not
  represent bundled Core's effective Browser/runtime policy.
- Replaced them with read-only, source-accurate status and routed approval
  policy to the existing Tools authority surface.
- Expanded Raw Engine Mode disclosure to cover the standalone Core config,
  active Desktop profile override, skills/specialists, and selected Desktop MCP
  connectors.
- The Overview and Profiles panes now show the producer-resolved active Core
  profile path and fail closed to `Path unavailable` instead of fabricating a
  default-profile or `~/.wayland-core/config.toml` location.
- Focused settings/retention proof passes 2 files / 36 tests, targeted lint with
  zero warnings and errors, TypeScript, and `git diff --check`.
- Receipt: `receipts/SBX-0-SETTINGS-TRUTH-CANDIDATE.md`.
- Core localhost grants, toolchain roots, workspace retention, packaged updater
  relaunch, and support-context continuation remain open; this correction does
  not represent them as fixed.

### M0A managed-workspace retention foundation — 2026-07-16

- Added a pure retention classifier plus a canonical-path filesystem collector
  with no filesystem/database mutation API.
- Referenced, scheduled, artifact-bearing, modified, user-promoted, foreign,
  incomplete, malformed, and unknown workspaces all fail closed to `preserve`.
- Only a fully proved empty app-managed shell beyond the visible retention
  window can become `quarantine-eligible`; that result is explicitly not
  deletion authority.
- The production projection joins conversations, Projects, schedules, and live
  worker processes. Artifact and receipt authorities remain explicitly
  unavailable, so production currently preserves every generated workspace.
- Storage exposes the trusted-local read-only dry run, shows what is protected
  and why, explains which authority inventories keep cleanup locked, and lets
  the user reveal a protected directory in Finder/Explorer. Paired WebUI access
  is denied and no destructive provider/control exists.
- Chat deletion no longer best-effort cascade-deletes scheduled tasks. It is
  blocked while a schedule references the chat and fails closed when schedule
  authority cannot be read; workspace files are untouched.
- Workspace migration no longer offers a destructive schedule-skip path.
  Schedule authority is preflighted, schedules move with the chat, partial
  moves are compensated, and the source remains until integrity passes.
- Team deletion now inventories schedules for every agent chat before mutation,
  blocks all user-created/unclassified schedules, removes only explicitly tagged
  team rituals, and re-inspects afterward. Schedule-authority failure and
  remaining jobs fail closed; failed chat deletion preserves the Team record.
  Workspace files and reports are never deleted, the confirmation names every
  destructive Team-owned record, and bridge refusal can no longer appear as UI
  success.
- The ownership, stable-snapshot, quarantine, restore, and permanent-deletion
  boundary is normative in `WORKSPACE-OWNERSHIP-CONTRACT.md`.
- Focused retention/lifecycle/bridge/UI proof passes 10 files / 109 tests, bounded targeted
  lint with zero warnings and errors, TypeScript, and `git diff --check`.
- Additional Team-retention proof passes 6 files / 67 tests, bounded targeted
  lint passes 5 files with zero warnings/errors, TypeScript, and
  `git diff --check`.
- Workspace file/folder removal now uses recoverable OS Trash and fails closed
  without a Trash provider; no permanent unlink/rm fallback remains on the user
  surface. Scheduled-task deletion stops future runs but preserves and detaches
  completed run chats, their workspace paths, and provenance instead of erasing
  them. Additional focused proof passes 7 files / 88 tests, TypeScript, and diff
  hygiene; legacy operation files' pre-existing lint warnings remain disclosed.
- Project references now use a project-local archive with a visible Restore
  journey instead of permanent unlink, including traversal/symlink/incomplete-
  entry fail-closed proof. Legacy imports on Desktop and hosted WebUI now create
  the same durable, atomic file-only safety export before overwrite and surface
  its exact recovery path. The irreversible reset provider and UI are disabled
  until the authoritative recovery-point gate is complete; a stale renderer
  cannot bypass that refusal. Focused storage/reference/reset proof passes 6
  files / 37 tests and current TypeScript passes.
- The workflow sidebar no longer offers an erasing “Delete workflow” action for
  clearing in-flight work. “Stop workflow” ends and hides the run while keeping
  its plan/progress row, parent chat, outputs, and files. Even the legacy delete
  provider now aliases to that non-destructive transition. The combined
  retention/storage/workflow matrix passes 9 files / 132 tests; TypeScript and
  diff hygiene pass.
- User-installed skills and custom-assistant rules/skills now move to
  recoverable OS Trash with no unlink/rm fallback. Assistant resource removal
  validates a single-segment ID and matches the exact filename family instead
  of compiling renderer input as a regular expression; unrelated resources
  cannot be broad-matched. Any resource-removal failure prevents the assistant
  configuration record from being removed. Focused proof passes 1 file / 27
  tests and the combined recoverability matrix passes 10 files / 137 tests;
  TypeScript and diff hygiene pass. The current UI goes further: “Archive”
  preserves the full assistant config and every resource in place, disables it,
  and restores through the existing Disabled-section enable toggle. It handles
  both assistant config families. Focused archive/compatibility proof passes 3
  files / 38 tests and TypeScript passes.
- Preview history no longer silently prunes versions beyond 50; atomic index
  writes and fail-closed validation prevent corrupt or path-escaping state from
  becoming an empty-history overwrite. Transient workspace snapshot cleanup
  now requires an exact ownership marker instead of a broad filename prefix.
  Workspace revert moves new and modified bytes to OS Trash before removal or
  baseline restore, refuses traversal/symlink escapes, and preserves the file
  if Trash is unavailable. Exact-current proof passes 4 files / 54 tests;
  bounded lint is zero-warning/zero-error; TypeScript and diff hygiene pass.
- Drop-folder ingest no longer destroys its source. The exact original is
  retained under `.processed/` before its top-level inbox name is removed, the
  depth-zero watcher cannot re-ingest the archive, and the UI discloses the
  recovery location. Exact-current import/index/UI proof passes 3 files / 34
  tests; bounded lint is zero-warning/zero-error; TypeScript and diff hygiene
  pass. Extension uninstall hooks do not delete extension directories; the
  builder removal path is transaction rollback for its own failed new target.
- Receipt: `receipts/M0A-WORKSPACE-RETENTION-CANDIDATE.md`.
- Artifact/receipt ledgers, baseline-aware scaffolding classification,
  explicit keep-forever promotion, recoverable quarantine/restore, and permanent-
  deletion policy remain open.

## Current evidence - 2026-07-16

### Green Desktop proof

- `bun run typecheck`: passed after the Cockpit selected-first agent control and test-harness repairs.
- `bun run lint`: zero errors. The repository still reports 2,639 pre-existing warnings; warning cleanup is not represented as complete.
- `bunx electron-vite build`: main, preload, and renderer production bundles passed. Existing third-party `use client`, mixed dynamic/static import, and chunk-size warnings remain visible.
- Full exact-current Vitest corpus after the M0A adversarial safety pass: 1,273 files passed, 19 skipped; 13,256 tests passed, 137 skipped; zero failures.
- Focused recovery/startup/packaging suite: 13 files and 67 tests passed.
- Focused Cockpit/navigation/agent/modal suite after the selected-first refinement: 4 files and 20 tests passed.
- Cockpit transition Electron suite: 4 of 4 passed, including absent-preference Classic default, in-place Cockpit opt-in, compact agent-roster disclosure, route preservation, power-destination reachability, and return to Classic.
- Combined live Electron route suite: 31 passed and 2 screenshot-only cases skipped; zero failures. It covers the Cockpit transition, primary navigation, and all 23 Settings routes.
- `git diff --check`: passed.

### M0A local engineering audit

- Startup now returns distinct typed stops for future schema, corrupt data, native-driver failure, and ordinary startup failure. Symlinked or non-regular physical DB paths refuse before the SQLite driver opens them.
- Recovery manifest validation now rejects non-canonical and duplicate artifact paths, malformed external references, unproven copy consistency, and logical reference/exclusion overclaims. Malformed serialized manifests fail closed as validation results instead of throwing.
- Recovery capture now reserves every sanitized destination and aborts all publication when distinct source names alias the same artifact path. Non-sensitive copies also use exclusive creation.
- Exact-current proof: focused recovery/database 39/39 plus one real-SQLite Bun test; full Vitest 13,256 passed and 137 skipped across 1,273 passed and 19 skipped files; typecheck, production build, targeted zero-error lint, and `git diff --check` passed.
- Receipt: `receipts/M0A-ENGINEERING-SAFETY-CANDIDATE.md`.
- A dependency-light production bootstrap command now verifies an externally supplied recovery snapshot without entering stateful startup. It refuses snapshot-root/manifest symlinks, checks manifest file identity around the read, verifies the full manifest/artifact corpus, and does not load or mutate CDP state. The built Electron smoke exits `2` with only typed invalid-snapshot JSON for a missing root.
- Exact-current proof after this slice: 1,294 files / 13,367 tests passed, 19 files / 140 tests skipped; TypeScript and the production Electron/Vite package build passed. Focused recovery/bootstrap proof passes 2 files / 11 tests.
- This is not M0A completion. Dependency-light production commands now create, verify, or materialize a recovery point, stream-download the exact current-platform Classic release asset, and prepare that asset into a private isolated runtime without loading ordinary app state or CDP. Download follows at most five HTTPS redirects restricted to GitHub release hosts, enforces the compiled size while streaming, verifies the digest, and exclusively publishes only the catalog asset. Preparation replays the release receipt, uses macOS `ditto`, a separately checksum-pinned packaged Windows `7za.exe`, or a direct Linux AppImage copy, then re-verifies the exact executable and platform publisher before writing a non-authorizing preparation receipt. `--launch-classic-recovery-snapshot <snapshot> --use-pinned-classic-release --recovery-destination <destination>` now collapses acquisition, extraction, publisher verification, authenticated materialization, schema transformation, neutralization, and isolated launch into one fail-closed operator action. It rejects mixed pinned/provided binary sources, removes the transient release archive on every outcome, removes an orphan prepared-binary runtime if snapshot preparation fails, retains complete receipts when a fully prepared recovery cannot spawn, and keeps malformed recovery option sets away from live CDP configuration. `--create-recovery-snapshot` owns the ordinary Desktop profile lock, inventories the live authority set, uses SQLite online backup, seals sensitive state, reads the actual source schema, and binds Desktop-owned copied files to a content-derived start/end mutation epoch. It deliberately fails closed when any Core default or named-profile state is present until producer-owned quiescence issue `#896` is accepted. An immutable compiled catalog now pins GitHub release `346809341`, tag commit `74b37efb1a1f624ec86b66fb3465025cb1a9fd22`, and the exact asset plus extracted executable identity for all six v0.11.8 targets; its canonical content digest is separately pinned in code. Every production binary-trust decision now returns a structured receipt binding that catalog digest, release/tag commit, selected asset, exact executable, and platform publisher evidence. The identical receipt is required before preparation, before atomic publication, and immediately before spawn; test-injected evidence has a separate contract with `authorizesClassicBinaryLaunch: false` and is runtime-forbidden outside tests. macOS and Windows publisher parsers require exact identity fields rather than substring matches. All six live downloads matched their asset pins. The production asset-to-binary preparation chain passed against the actual macOS ARM64 release ZIP without starting Classic. macOS ARM64/x64 ZIP extraction produced the pinned executable hashes and both bundles passed deep strict code-signature validation for `Developer ID Application: Ferrox Labs, LLC (PX6SP9GPWJ)`, bundle ID `com.ferroxlabs.wayland`, Team ID `PX6SP9GPWJ`, and stapled notarization; Windows ARM64/x64 NSIS extraction produced the pinned PE hashes; Linux AppImages are the pinned executable artifacts. The production launcher no longer executes Classic for version discovery: exact compiled executable bytes plus the current platform's publisher gate replace the unsafe `--version` probe. A real macOS ARM64 app package now proves that both checksum-pinned Windows extractor resources physically land in `Contents/Resources` and pass the post-package gate. Windows/Linux packaged-host receipts, recovery-specific Windows Authenticode, Linux publisher signing, and the six-target rollback/boot/re-upgrade matrix remain absent.

### Live visual proof

- `../cockpit-chat-live.png`: live Cockpit home with the selected-first agent control.
- `../cockpit-agent-picker-live.png`: the same live renderer with the full agent roster opened.
- `../mockups/mission-run/index.html`: adaptive multi-step run showing one correlated state as an in-thread activity spine plus a Plan/Outputs/Context mission rail, including an explained replan, active output, authority checkpoint, session-usable connection truth, and authoritative cost label.
- CDP evidence from the live development renderer: shell `cockpit`, one compact picker, selected summary `Wayland Core 18 agents`, one visible menu, and 19 visible items including agent discovery.

### Safety gaps closed during verification

- Repaired the channel-modal DOM test race by hoisting storage mocks, importing the surface once, and using deterministic render/cleanup. The isolated test passes repeatedly and the full corpus is green.
- Removed real database/model-registry initialization from the standalone bridge unit test through complete mocks; the test now proves bridge wiring without native ABI or filesystem contamination.
- Made the Windows NSIS cleanup release-track-aware on x64 and arm64. Stable removes only Stable protocol/application registry keys; Preview removes only Preview keys. Side-by-side uninstall no longer cross-deletes the other track.
- Cockpit keeps the current agent explicit and every configured agent one click away while Classic retains its full horizontal agent rail.
- Cockpit now presents outcome intents before assistant selection while keeping the full selected-first agent roster immediately available; this preserves power without making agent choice prerequisite configuration.

### Cowork follow-on audit

- Added `../COWORK-DEEP-DIVE.md` as the source-of-truth follow-on programme for provider-neutral knowledge work, source provenance, native artifacts, validation, iterative editing, connectors, schedules, and hosted Pro boundaries.
- Source tracing found that Cowork is currently a Gemini-backed preset over document skills rather than a cohesive work mode. Projects, preview/history, Office discovery, and skill search are strong reusable foundations.
- The audit found a release-level OfficeCLI contradiction at the shipped baseline: Desktop pinned `0.2.79`, while enabled Cowork skills described a materially different native API. The working tree has removed that npm/hosted-credit path and now pins native `v1.0.136`; this bullet records the historical defect, not current behavior.
- The audit found that Cowork overloaded a product-mode name with a workspace trust level, while the corresponding toggle was disconnected. The working-tree correction now uses `ask` / `trusted-edits`, migrates legacy values without changing effective approvals, and removes the dead toggle. Selecting Cowork does not widen filesystem, execution, or network authority.
- Began C0 without changing Cowork's backend or document dependency: rewrote the active Cowork rule around outcome, working set, acceptance, capability preflight, source provenance, format-specific validation, explicit authority, and hosted cost disclosure; removed the divergent legacy `skillFiles` binding so startup deletes the stale cached manual and the modern enabled-skill system is authoritative.
- Added Cowork and resource-resolution contract tests. Focused Cowork/preset-resource/preset-locale/assistant-utility coverage passes: 4 files and 50 tests. The full exact-current corpus, typecheck, production Electron build, and `git diff --check` pass after the C0 guardrail change.
- Added a bounded, cached OfficeCLI authoring-contract probe that distinguishes native authoring from the separate `officecli watch` preview path. Readiness requires exact `1.0.136` plus the complete resident `create`, `open`, `close`, `add`, `set`, `query`, `validate`, and `view` command surface; missing executables, version drift, incomplete probes, and incompatible APIs fail closed without throwing.
- Cowork now always receives the live capabilities manifest and requests the Office authoring readiness line. Concierge remains always-on without the Office-specific line; other assistants retain capability-intent-only injection. Both persistent assistants suppress duplicate per-turn adverts.
- The live probe against the exact bundled executable reports `0.2.79`, `incompatible`, `hosted-credits`, and all eight resident native commands absent. Cowork is explicitly instructed not to invoke the incompatible authoring API. Preview code was not changed or disabled.
- Focused readiness/Cowork coverage passes: 4 files and 72 tests. Exact-current typecheck, full Vitest (1,266 passed files / 13,187 passed tests), production Electron build, live registered-platform probe, and `git diff --check` pass.
- Added explicit authority vocabulary and migration coverage: stored `chat` / `cowork` values map to `ask` / `trusted-edits`, and the next local access write rewrites the complete map canonically. The legacy key/wire namespace stays stable so upgrades and remote-denial policy do not regress.
- Removed the disconnected `CoworkToggle.tsx`. A new isolation guard proves the Cowork preset carries no access grant and no renderer production path calls `workspaceTrust.set`; any future explicit access-control UI must change that contract deliberately.
- Pinned non-escalation across ACP, OpenClaw, Gemini, and Core plus remote WebSocket/config tamper denial. Focused authority/Cowork readiness coverage passes: 11 files / 100 tests. Exact-current typecheck, full Vitest (1,267 passed files / 13,193 passed tests), production Electron build, and `git diff --check` pass.
- Removed the disconnected Markdown-centric `DocumentConverter` and four uncalled conversion writer/export methods that falsely suggested native round-trip capability. The remaining registered service is explicitly extraction-only: Word-to-Markdown, Excel-to-JSON, and PowerPoint-to-JSON. No renderer caller exists, so it is not represented as a Cowork journey.
- Closed a remote local-file read primitive: `document.convert` accepts a local path and returns extracted contents, but the default-allow paired WebSocket policy did not deny it. The entire `document.*` namespace is now remote-denied, including future converter keys.
- Added bridge contract coverage for supported extensions, mismatched-file fail-closed behavior, unknown targets, correct extraction routing, and remote denial. Focused conversion/security coverage passes: 5 files / 26 tests. Exact-current typecheck, full Vitest (1,268 passed files / 13,201 passed tests), production Electron build, and `git diff --check` pass.
- Replaced the contradictory authoring dependency with a checksum-pinned native `iOfficeAI/OfficeCLI` `v1.0.136` release corpus covering macOS, Windows, Linux, and Alpine Linux on x64/ARM64. Build preparation verifies the selected asset before copy or execution; packaging makes the binary and proof manifest mandatory. The hosted npm `0.2.79` dependency/trust/package/PATH fallback is removed, and Wayland-managed processes disable upstream background updates.
- Removed mutable OfficeCLI bootstrap instructions from every bundled Office and knowledge-work skill. A recursive regression gate rejects curl, npm-global, moving-latest, and install-on-demand recovery language; missing or incompatible native authoring now fails closed to a Wayland reinstall/update message.
- Expanded `contracts/officecli/v1/contract.json` to cover every concrete DOCX, XLSX, and PPTX help element referenced across the base and specialist skill library. Compatible build-host binaries must pass that executable schema contract plus real create/mutate/query/validate/view journeys for all three formats before packaging.
- Native macOS ARM64 proof passed at exact `v1.0.136`: hash/version, ready/local classification, DOCX/XLSX/PPTX executable journeys, and a reachable `officecli watch` preview. The mandatory proof now exercises the financial-model, data-dashboard, Word-form, and pitch-deck packs through formulas, names, validation/formatting, charts, content controls, legacy fields, protection, connected shapes, notes, and embedded charts. The focused Office/Cowork matrix passed 11 files / 125 tests; full Vitest passed 1,272 files / 13,237 tests with 19 files / 137 tests skipped; typecheck, production Electron build, targeted zero-error lint, and `git diff --check` passed.
- Cowork's named local work is bounded C0-A evidence for contract truth, provider neutrality, authority isolation, conversion reachability/security, and native OfficeCLI distribution on the current macOS ARM64 build host. The prior C0 closure claim is superseded: hosted fallback removal and the preliminary executable ledger are locally implemented, but C0-B remains locked until M2 publishes the sole shared readiness schema, signed `v0.11.8` authority downgrade/re-upgrade proof passes, and target-exact component/package evidence exists. C1 waits for C0-B. Final C0 release closure is an M8 output after signed six-target replay of C0-B and C1; it is not a C1 prerequisite.
- Packaged-resource verification now requires the exact declared OfficeCLI target runtime (`darwin|linux|win32` plus `x64|arm64`) and matches its manifest platform/architecture. A valid binary for the wrong target, or a verifier invocation without an explicit target, fails closed. Focused preparation/release/verifier proof passes 3 files / 14 tests.
- A real macOS ARM64 package now preserves the exact OfficeCLI release SHA-256 `b8582853cc464fa0bdb2fabc2803821472c9449c38b365a7be79fcb53d6356e7` and upstream Aion Developer ID signature, retains hardened runtime and only `com.apple.security.cs.allow-jit`, passes deep app-signature validation, and passes all critical resource checks. Focused OfficeCLI/package supply-chain proof passes 2 files / 17 tests. The Wayland app is locally ad-hoc and other package targets remain unproven, so C0 is not release-closed.
- The same package run exposed a fail-open Core provenance defect. **Closed locally in Cycle 10:** direct packaging now forces exact Core `v0.12.25` preparation with independent archive and extracted-binary pins, prunes stale runtime files, isolates one platform/architecture per invocation, rejects every local/skipped/unverified/mismatched/self-asserted receipt, and replays the actual packaged executable bytes. Focused proof passes 5 files / 46 tests; the bounded Gemini rerun reports zero High. The macOS ARM64 local package is partial evidence only: six-target signed packaging and release receipts remain open in M0A/M8.
- The fresh post-correction macOS ARM64 package preserved the exact pinned Core SHA-256 `aa818a9492b59fd4402b2d4d451104d88dee5e5c20f05b722a487cdc39a6a382`, contained exactly `wayland-core` plus `manifest.json`, passed the standalone target/release/archive/binary replay, and passed deep strict app-signature validation.

### MCP operational-truth correction

- Direct customer evidence names Tavily, Firecrawl, n8n, and Beeper as configured/enabled in the Library while the active assistant's ToolSearch reports no matching tools. This is P1 release-blocking evidence, not a cosmetic status-label defect.
- The automatic connector-fingerprint session rebuild and persisted Core session-readiness implementation were outside the pre-M0A/M1 exception. Both are now test-harness-only: activation requires the explicit flag, `NODE_ENV=test`, and authoritative `isPackaged=false`. Development and every packaged application are denied.
- The composer no longer reloads legacy persisted `mcpSessionState` or synthesizes readiness from uncorrelated raw `mcp_ready`/`mcp_failed` events. Only the preview-gated complete process snapshot can drive the dormant receipt treatment; the ordinary list says “Configured”, the live scope says “Selected for this chat”, and neither phrase claims tool availability.
- A physical-project Core launch lease resolves and captures one canonical workspace for the lease, temporary `.wcore.toml`, child `cwd`, engine ready, and restore. Symlink aliases serialize and alias retargeting cannot redirect an acquired launch. Atomic journal recovery preserves newer user bytes, flushes rename/unlink metadata in order, and refuses final-component symlinks for the target, marker, and backup.
- Every Core config writer shares one atomic mutation lock. The manager captures one active-profile home before MCP publication and carries it through engine spawn; corrupt or unreadable profile markers fail closed, and marker activation is an atomic synced replacement.
- Gemini replacement reconstructs its worker fork only after confirmed old-worker exit; Electron timeout/refusal rejects replacement instead of permitting overlap. Unsolicited connector receipts are rejected by both reducer and presentation layers, but definition/session/scope correlation remains MCP-2 and cannot promote any product UI.
- Latest WCore publication/launch proof passes 7 files / 69 tests and marker/config/lease security proof passes 7 files / 48 tests. Exact-current full proof after the recovery-verifier slice passes 1,294 files / 13,367 tests, with 19 files / 140 tests skipped; typecheck and the production package build pass. Independent Claude and Gemini exact-current audits each reported 0 Critical / 0 High for the bounded Cycle 8 corrective tree; the later recovery-verifier delta has focused, full-corpus, typecheck, package, and real-process smoke proof but is not represented as re-reviewed by those external auditors. This does not claim the four named vendors have passed live ToolSearch/invocation canaries or unlock MCP-2/product readiness promotion.

### Core v1 validation candidate

- Imported the exact 110-fixture `wayland-desktop-core` `1.0` corpus from local Core commit `d0aa0abc75afe056cc5434fcd652efa6d474ab0c` without reading or consuming the dirty Core checkout.
- Pinned generator `wcore-desktop-contract-gen/1` and the fixture, schema, and source-input digests in runtime code, tests, and `contracts/wayland-desktop-core/v1/PRODUCER-PIN.md`.
- Recomputed the fixture and schema digests from vendored bytes and replayed all 11 commands, 39 canonical events, compatibility fixtures, and every adversarial policy/workflow/Anvil family through the actual Desktop consumer and semantic reducers.
- Replaced raw `readline` JSON parsing with an 8 MiB bounded fatal-UTF-8 JSONL decoder on the production WCore stdout boundary. Critical/unknown-criticality events, malformed frames, unsupported required extensions, descriptor drift, gaps, conflicting duplicates, out-of-order evidence, and conflicting terminals fail closed.
- Implemented `ordinary_turn_tool_replay_reducer`, `anvil_desktop_replay_reducer`, and `anvil_persistent_mutation_watcher`. Publication-bound Anvil trust becomes historical after mutation, watcher failure/unavailability, or disconnect and requires fresh Core validation.
- Added host-derived `anvil_trust_changed` propagation so the UI can remove a live trust treatment without Desktop forging Core invalidation evidence. Host-delegated channel and scheduling ownership is unchanged.
- Core published exact producer commit `d0aa0abc75afe056cc5434fcd652efa6d474ab0c` on `FerroxLabs/wayland-core` / `origin/feat/887`; Desktop verified remote reachability and no longer depends on a dirty or local-only checkout. Production binary preparation remains separately pinned to released Core `v0.12.25` pending an authorized binary/release compatibility uptake.
- Exact-current proof: contract/raw-wire/manager focus 54/54; WCore/Desktop integration 483 passed and 3 skipped; full Vitest 13,219 passed and 137 skipped across 1,269 passed and 19 skipped files; typecheck, production Electron/Vite build, targeted zero-error lint, and `git diff --check` passed.

### Flux routing-evidence producer acceptance

- Accepted Flux PR `TradeCanyon/flux-router#142` at exact merged commit `6e7fa427ee6c062b531e67c0e7e1cedfd7aff208`, independently pinned from behavioral baseline `6eda360c8497b4dab0e26003ad7bad03d0aa9c6f`.
- Vendored the exact contract `1.0.0` / generator `1.0.1` corpus: 12 fixtures, schema digest `356fb1f472c9909bc5a11003edf8e4e30e93ad3d06c8607316973acfb490e85a`, and bundle digest `128b817984ff2bb76b8fd8c10d1f35b4ce3ac24498fe1f693068fc4e6d1ce9fd`.
- The drift gate recomputes every file digest and the aggregate bundle digest, rejects missing/extra files, and replays all canonical fixtures through the Desktop capability evaluator or evidence reducer.
- Disconnected and invalid-credential fixtures now consume the producer's corrected `no_flux` expectation directly; the former Desktop filename exception is gone.
- Strengthened the reducer to mirror producer ordering/reference semantics and fail closed on malformed data, correlation drift, gaps, conflicting duplicates, out-of-order/post-terminal evidence, invalid attempts/retries/fallbacks/cost/terminals, and explicit-native fallback.
- Exact-current proof: focused contract/composition roots 12/12; Flux/provider/model-selection surface 145/145; full Vitest 13,246 passed and 137 skipped across 1,273 passed and 19 skipped files; typecheck, production Electron/Vite build, targeted zero-warning lint, and `git diff --check` passed.
- Receipt: `receipts/M1F-FLUX-ROUTING-EVIDENCE.md`.

### Gates still open

- `FerroxLabs/wayland#887` remains open by coordination policy, but Core's exact `d0aa0abc75afe056cc5434fcd652efa6d474ab0c` producer commit is published and Desktop has accepted it: all 110 fixtures replay through the production consumer/reducers, all three deferred Desktop proofs are implemented, and raw stdout/IPC forwarding checks pass. Packaged Core remains released `v0.12.25` until a separately authorized binary/release compatibility uptake.
- `FerroxLabs/wayland#888` remains open by coordination policy, but its merged producer corpus is now pinned, byte-verified, replayed, and accepted by Desktop. Electron/Desktop and standalone Web/Cloud now initialize separate atomic fail-closed adapters, replay the complete serialized request corpus, and revoke all claims on later capability/stream failure. The full exact-current suite passes 1,273 files / 13,246 tests with 19 files / 137 tests skipped; the 17-file Flux matrix passes 145 tests; typecheck, targeted lint, production build, and `git diff --check` pass.
- Full M1F remains open only at the real producer-delivery boundary: merged Flux commit `6e7fa427` publishes no live endpoint or stream framing. SC-14 route explanations, provider attempts, and cost claims therefore remain disabled until a trusted Flux transport feeds a current capability response and complete correlated stream into the registered adapter. The disconnected, test-only `FluxDesktopService` is not promoted into a false evidence source.
- The customer credit-depletion report adds a second non-negotiable live boundary rather than changing the accepted fixture result: Flux's authoritative per-request charge must survive Core/ACP adapters with request/attempt/retry/fallback correlation and reconcile to the conversation total. Current Mission Control rows are backend-dependent aggregates or estimates and are not accepted as account-debit proof. SC-14C/J24 stay red until that live chain passes; no customer identity or account data enters public receipts.
- ACP cost truth is now consistent across ordinary chat and Teams: context-window occupancy never becomes processed/billable token spend, only finite non-negative USD cumulative cost may produce a session delta, and legacy Team context rows no longer inflate token totals. Focused Team writer/renderer proof passes 2 files / 91 tests; exact-current TypeScript, targeted zero-error lint, and `git diff --check` pass. This closes a local false-accounting defect but does not turn estimated Desktop rows into Flux debit receipts or satisfy SC-14C/J24.
- Live Anvil acceptance canaries remain blocked on Keychain injection of `AnvilTestKey2`; this does not weaken deterministic fixture acceptance.
- The bounded external Classic launcher is registered in the dependency-light production bootstrap for both an already materialized tree and a sealed recovery snapshot. The pinned-release snapshot mode automatically acquires the exact compiled asset, performs trusted platform extraction and publisher verification, authenticates OS-credential-store envelopes, validates the schema-53 tree, removes transient plaintext and release-archive state, then launches only the isolated schema-52 result. The registered capture command handles Desktop-owned state under the live profile lock but refuses any present Core state until `FerroxLabs/wayland#896` supplies a producer-owned quiescence lease; it does not fake multi-file Core consistency with timestamps or process killing. The v0.11.8 catalog establishes exact six-target release-asset and executable identities. Release-asset and preparation receipts remain explicitly non-authorizing; launch requires exact compiled executable bytes and the current platform publisher gate before preparation, publication, and spawn. On macOS that gate validates the complete app bundle, Developer ID authority, Team ID, bundle ID, and stapled ticket without starting Classic; on Windows it requires valid Ferrox Labs Authenticode; Linux currently has only exact release/executable digests. The real macOS ARM64 package proves the two Windows extractor resources are present and exact; it does not prove a packaged recovery journey. Recovery-specific Windows/Linux publisher receipts and six-target signed install, recovery, rollback, boot, and re-upgrade receipts do not exist. Therefore M0A is not complete.
- M0B now has a closed, content-free event contract, deterministic local/structured-UAT aggregator, and consent-gated recording service. It separates novice, knowledge-work, developer, and operator cohorts; exposes incomplete sessions and unresolved journeys; measures explicit crash-free sessions and journey-start denominators; rejects unknown or content-bearing fields before storage; exports aggregates without raw identifiers; and stops on the five zero-tolerance incident classes. The unsigned default policy names Sean as decision owner, requires 20 total participants, 5 per cohort, and 10 starts per primary journey, and keeps invited alpha disabled. Focused proof passes 22 tests plus TypeScript and zero-warning targeted lint. Product repository wiring, consent/visibility UI, the actual 14-calendar-day Classic observation, and a signed `M0B.json` remain absent, so no invited alpha is authorized.
## Retention hardening — Memory archive and wiki boundary

- [x] Replace the visible permanent Memory deletion promise with Archive.
- [x] Persist the complete original memory block and SHA-256 before active-source mutation.
- [x] Keep the original source file when its final entry is archived.
- [x] Expose Archived memories and in-app restore from the Memory top bar.
- [x] Fail closed on tampering, path escape, ambiguous archive identity, and summary collision.
- [x] Serialize edit/archive/restore so concurrent read-transform-write operations cannot stomp state.
- [x] Deny remote edit/archive/restore authority while preserving read-only views.
- [x] Prove wiki undo deletes only the derived promoted copy; source memory remains.
- [x] Search the active Memory service tree for compaction/pruning; none exists.
- [x] Focused proof: 4 files / 99 tests, TypeScript, and diff check pass.

This slice is code-present and focused-proof green. It does not accept M0A,
authorize release/cohorts, or relax any recovery, producer, MCP, packaging, or
rollback gate.

## Retention hardening — Scheduled-task archive and restore

- [x] Replace permanent schedule-row deletion with a durable archive record.
- [x] Copy and byte-verify the complete per-job skill tree before row removal.
- [x] Move the original skill directory into the archive instead of recursively deleting it.
- [x] Keep the live row and restart an enabled timer when archive or database mutation fails.
- [x] Route automatic orphan cleanup through the same archive boundary.
- [x] Preserve completed chats, reports, workspaces, and `archivedCronOrigin` provenance.
- [x] Expose Archived schedules and in-app Restore from Automations.
- [x] Restore the schedule paused and require explicit user resume.
- [x] Fail closed on path escape, symlink, record tampering, skill tampering, and target collision.
- [x] Preserve restored bytes inside the archive when database insertion fails.
- [x] Deny remote restore authority while allowing non-secret archive inventory.
- [x] Focused proof: 6 files / 165 tests, TypeScript, bounded zero-error lint, and diff check pass.

This slice is code-present and focused-proof green. It does not accept M0A,
authorize release/cohorts, or relax any recovery, producer, MCP, packaging, or
rollback gate.
