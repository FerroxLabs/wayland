---
phase: WLD-01-safety-foundation
plan: 22
type: execute
wave: 6
depends_on: ["01-06", "01-33"]
files_modified:
  - contracts/recovery/state-authority-ledger.json
  - src/process/services/recovery/authority/authorityAdapters.ts
  - src/process/task/GeminiAgentManager.ts
  - src/process/task/AcpAgentManager.ts
  - tests/unit/process/services/recovery/authority/authorityAdapters.test.ts
  - tests/integration/recovery/recoveryBackendAuthorityAdapters.test.ts
autonomous: true
requirements: [SAF-01, SAF-03, SAF-05]
must_haves:
  truths:
    - "Core plus Gemini and ACP have explicit session/handle, capture, resume, degraded, and validation authority."
    - "Desktop SQLite and Office/artifact workspaces remain explicitly inventoried state authorities, not substitutes for backend adapters."
    - "An unproven or non-resumable backend is excluded from the cohort rather than normalized into success."
  artifacts:
    - path: contracts/recovery/state-authority-ledger.json
      provides: backend and storage authority map including Core, Gemini, ACP, Desktop SQLite, and Office/artifact state
  key_links:
    - from: contracts/recovery/state-authority-ledger.json
      to: src/process/services/recovery/authority/authorityAdapters.ts
      via: closed adapter IDs and producer-specific mutation epochs
---

<objective>
Seal the state-authority ledger for Core, two representative non-Core backends, and supporting Desktop-owned stores.

Terminal claim: Core, Gemini, and ACP each bind backend-owned session/handle authority to exact capture/resume/degraded behavior, while Desktop SQLite and Office/artifact stores remain separately inventoried; any unproven backend is excluded from the cohort.
</objective>

<execution_context>
This extends the inventory contract without weakening #896: Core remains producer-quiescence dependent. Representative non-Core backend adapters are Gemini and ACP based on current production manager wiring. Desktop SQLite and Office/artifact workspaces stay in the broader storage ledger. Unknown producers remain unavailable.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Implement and attack the closed authority-adapter ledger</name>
  <files>contracts/recovery/state-authority-ledger.json, src/process/services/recovery/authority/authorityAdapters.ts, src/process/task/GeminiAgentManager.ts, src/process/task/AcpAgentManager.ts, tests/unit/process/services/recovery/authority/authorityAdapters.test.ts, tests/integration/recovery/recoveryBackendAuthorityAdapters.test.ts</files>
  <action>Define exact adapter IDs, producer owner, backend session/handle identity, resumability authority, mutation epoch source, quiescence/capture semantics, restore owner, validation receipt, secret disposition, and failure behavior for Core, Gemini, and ACP. Wire Gemini/ACP terminal/session-handle facts into the adapters without treating process existence as resumability. Keep Desktop SQLite and Office/artifact state as explicit separate storage authorities. Require ledger/runtime lockstep and reject unknown producers, generic filesystem-copy substitution, absent/expired/mixed epochs, wrong handles/owners, partial roots, adapter self-assertion, and mutation after quiescence. When resume is unsupported or unproven, expose read-only/degraded/non-resumable and mechanically exclude that adapter from cohort eligibility. No new locale/generated files are owned.</action>
  <verify>GSD_RUNTIME=codex bunx vitest run tests/unit/process/services/recovery/authority/authorityAdapters.test.ts tests/integration/recovery/recoveryBackendAuthorityAdapters.test.ts && bun run typecheck</verify>
  <done>Core/Gemini/ACP adapter authority is explicit, non-Core degraded behavior is truthful, and unproven backends cannot enter M0A/cohort claims.</done>
</task>
</tasks>

<threat_model>
Producer/adapter identity blocks spoofing; ledger lockstep and epochs detect tampering; validation receipts support attribution; secret disposition avoids disclosure; unknown producers cannot elevate into recoverable state.
</threat_model>

<verification>
Run `bun run lint -- src/process/services/recovery/authority/authorityAdapters.ts src/process/task/GeminiAgentManager.ts src/process/task/AcpAgentManager.ts tests/unit/process/services/recovery/authority/authorityAdapters.test.ts tests/integration/recovery/recoveryBackendAuthorityAdapters.test.ts`, focused/integration tests, typecheck, and exact full `bun run test` before summary or commit.
</verification>

<success_criteria>
M0A can prove or explicitly exclude Core, Gemini, and ACP backend session authority without confusing storage roots for backend adapters.
</success_criteria>

<output>
Create `01-22-SUMMARY.md` only after the full gate, naming each authority, owner, epoch, and deferred #896 blocker.
</output>
